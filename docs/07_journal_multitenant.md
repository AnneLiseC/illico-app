# 07 — JOURNAL MULTI-TENANT BATILIS (archive)

 Date | Étape | Statut |
|---|---|---|
| 28/05 | Cadrage modèle cible (D1-D17) | ✅ verrouillé |
| 28/05 | Audit 1 (fonctionnel) | ✅ |
| 28/05 | Audit 2 (données & sécurité) | ✅ vérifié en base |
| 28/05 | Audit 3 (ampleur/chiffrage/séquençage) | ✅ |
| 28/05 | Plan d'exécution 6 phases (L1-L21) | ✅ |
| 28/05 | **L9 Rebranding BATILIS** | ✅ mergé |
| 28/05 | **L13 Création chantier sans client + login** | ✅ mergé `3dbd6f1` |
| 28/05 | **L11 Neutralisation Msg + Stats** | ✅ mergé `f12420a` |
| 28/05 | **L17 Durcissements (vérifiés/dégonflés)** | ✅ traité |
| 28/05 | **PHASE 1 COMPLÈTE** | ✅✅ |
| 28/05 | Phase 2 — SQL versionnés `docs/sql/MT1-MT4` (commit `f905c2b`) | ✅ |
| 28/05 | **L1 — MT1 tables societes/agences créées + RLS fermée** | ✅ appliqué main | 
| 28/05 | **L2 — MT2 seed CTP+Martigues + colonnes nullable** | ✅ appliqué main |
| 28/05 | **L3 — MT3 backfill 102 lignes + NOT NULL** | ✅ appliqué main |
| 28/05 | **L4 — MT4 helpers + REVOKE anon/PUBLIC** | ✅ appliqué main |
| 28/05 | **PHASE 2 COMPLÈTE (côté schéma)** | ✅✅ |
| 28/05 | Découverte : NOT NULL casse les INSERTs sans `agence_id` | ⚠️ acté, plan ajusté |
| 28/05 | **L6-light** (Phase 2-bis) : patch INSERTs + `api/create-agente` (commit `c385bf7`) | ✅ testé 9 chemins |
| 28/05 | **MT5** : policies lecture `societes`/`agences` (débloque admin) | ✅ appliqué main |
| 28/05 | **PHASE 2 RÉELLEMENT BOUCLÉE — app fonctionnelle** | ✅✅✅ |
| 29/05 | Correction donnée Marine : `2026-AM-021` rattaché à LEULIER Laura (UPDATE encadré, client_id était NULL) | ✅ fait |
| 29/05 | **Phase 3 démarrée** — audit RLS complet (patchwork 3 générations confirmé) | ✅ |
| 29/05 | **L5a-1-prep** (code) : lectures profiles compatibles policy stricte (commit `ddaa334`) | ✅ mergé |
| 29/05 | **L5a-1a** : policies profiles (SELECT cloisonné société, agente=soi / admin=sa société) | ✅ appliqué main |
| 29/05 | **L5a-1b** : trigger `profiles_protege_identite` (role+societe_id figés via app, souple en SQL direct) | ✅ appliqué main |
| 29/05 | **L5a-2** : RLS racines — dossiers (référente+agence), clients (référente+agence, trou « agente voit tous clients » fermé), artisans (annuaire société) | ✅ appliqué main, testé |
| 29/05 | **L5a-3-mig** : migration `redevances.agence_id` (ADD+backfill+NOT NULL, prépa multi-agence) | ✅ appliqué main |
| 29/05 | **L5a-3-rls** : RLS finances (5 tables : filles via EXISTS dossiers, factures_agente + redevances par agente_id) + trigger D17 patché + L6-light-bis (upsert redevance câblé agence_id, commit `0ae9c9c`) | ✅ appliqué main, testé |
| 29/05 | **L5a COMPLET** (fondations + racines + finances) — bug CTP identifié = affichage (lot code séparé) | ✅✅ |
| 29/05 | **MT6** : dénormalisation `societe_id` sur 4 racines (colonne+backfill+NOT NULL, trigger générique immuabilité société, policies basculées sur societe_id) | ✅ appliqué main, testé table par table |
| 29/05 | Nettoyage données test : 6 clients + 4 dossiers test supprimés (audit enfants 0, transaction encadrée). Base = 19 clients / 20 dossiers, que du réel | ✅ |
| 29/05 | **L5a-3-fix** : trou client filles finances fermé (devis/factures_artisans/suivi → staff only) | ✅ appliqué main |
| 29/05 | **L5b** : RLS 6 tables opérationnelles (staff via EXISTS dossiers ; branches client lecture seule sur comptes_rendus + photos ; photos nettoyé de 5→2 policies) | ✅ appliqué main, testé |
| 29/05 | **L5c** : RLS transverses (google_tokens par user, specialites catalogue global verrouillé, artisans_specialites + fiches_techniques via EXISTS artisans, messages staff+client) | ✅ appliqué main, testé |
| 29/05 | **L5c-B-fix** : régression objectif d'agence détectée (KPI à 0) → migration objectifs_ca (agence_id+societe_id) + policy à 3 branches (agente: perso+agence ; admin: société) | ✅ appliqué main, testé |
| 31/05 | **L8** : référence chantier par agence — UNIQUE (agence_id, reference) + fonction/trigger SECURITY DEFINER (génération côté base, max+1 par agence+année, bypass RLS) + patch code (suppression genererReference JS) | ✅ appliqué main + branche code, testé (création AMO → 2026-AM-023) |
| 31/05 | **Lot code masquage toggle CTP côté agente** (bug affichage) — `PillToggle` admin-only, agente forcée mode `agence` | ✅ branche `claude/laughing-mccarthy-oYec4`, testé Vercel |
| 31/05 | **PHASE 3 COMPLÈTE** (hors notifications reportée Phase 6) → place à la Phase 4 | ✅✅✅ |
| 31/05 | **MT7** : artisans société-wide — retrait de `agence_id` (trigger artisans + FK + index + colonne) + code fournit societe_id direct. Correction modèle multi-agence | ✅ code `072ccd7` + base `15161ed`, testé |
| 31/05 | **L6** (réduit) : `artisans/nouveau` + `clients/nouveau` lisent le profil via `useAuth()` (re-fetch supprimé). 3 autres pages en dette assumée | ✅ commit `8d98a44`, testé Vercel |
| 31/05 | **L7** : navbar nom d'agence/société dynamique (enrichissement `useAuth()` → `displayAgenceName`) + prénom redondant retiré | ✅ commits `96e8872`+`f3ecef0`, mergé `10734ca`, testé |
| 31/05 | **UPDATE valeurs CTP** en base (societes nom/siret 14ch/rcs + agences adresse/tél) | ✅ commit doc `b48d4b7` |
| 31/05 | **L10-a** : params bloc Agence dynamique + login neutre + texte profil retiré + adresse décomposée (SQL `code_postal` `8896618` + code `399afc9`) | ✅ mergé `23b4d97`, testé |
| 31/05 | **L10-b** : cron relances — villes d'agence dynamiques (boucle acompte) + décennale signée par admin société + villes. ⚠️ emails non branchés → validé build+diff | ✅ `5d1ab4b`, mergé `ff4b678` |
| 31/05 | **[HORS SPRINT — demande Marine] Modals de saisie : plus de fermeture au clic extérieur** (DevisModal puis 5 autres : ModalShell interventions/CR, agente créer/suppression, RDV planning, ModaleChoixClient). Retrait handler overlay + stopPropagation. Échap ModalShell conservé. Visionneuses/quick menu volontairement NON touchés. | ✅ `9d70cb3` (devis) + `77d2202`→`dde285a` (reste), testé |
| 31/05 | **L10-c1** : PDF `route.js` — nom d'agence dynamique (header + footer récap + footer CR), dossier enrichi agence+société. Frais conso/honoraires intacts | ✅ `3ad345c`, mergé `7d7bd20`, testé (récap+CR) |
| 01/06 | **L10-c2** : PDF `restitution.js` — (A) 7 `Ftr` + `makeCoverPage` dynamiques (`95915d4`) ; (B) suppression code mort buildDossierRestitution/R3, 233 lignes (`4a83e91`). **→ L10 COMPLET** | ✅ testé (DossierSuivi) |
| 01/06 | **Écrans saisie objectifs CA** : finances → lecture seule (suppr. `sauvegarderObjectif`, 2 `ObjectifBar` en `canEdit=false`) ; saisie centralisée dans paramètres (objectif agence volet Équipe + objectif agente en édition, helper client avec `agence_id`) ; insert objectif agente à la création via `api/create-agente` (agence résolue = unique agence société, tolérance échec partiel). Bug `sauvegarderObjectif` sans `agence_id` réparé au passage. Testé en base (ligne agente cible OK : montant/agence_id/societe_id corrects). **→ PHASE 4 COMPLÈTE** | ✅ mergé `4cc2a6` |
| 01/06 | **L14a — fix reconnexion après invitation** : page `/auth/set-password` (flow implicit, session auto + `updateUser`) + `redirectTo` corrigé dans `create-agente`. Prérequis `NEXT_PUBLIC_APP_URL` créée dans Vercel. Testé prod bout en bout (invitation → mdp → reconnexion OK). **→ PHASE 5 COMPLÈTE — PRÊT POUR LE TEST UTILISATEUR** | ✅ mergé `a9dc8b4` |
| 01/06 | **L20** : fix navigation menu client — Modifier (404 → `/clients/[id]?edit=list` ouvrant le mode édition) + Voir les dossiers (0 masqué / 1 → dossier / N → fiche client) | ✅ FAIT |
| 01/06 | **Audit 4 (sécurité) + Audit fonctionnel** post-phases — état réel vérifié. Sécurité : P0-9-bis fermé, get_my_role durci, L12 + bug unicité redevances confirmés bloquants. Fonctionnel : aucun bloqueur dur nouveau (login/création/paramètres OK) ; découvertes = recalculs hors finance.js (prioritaire), types RDV incomplets, bug Google TDZ, boutons morts finances | ✅ audits lecture seule, rien modifié |
| 01/06 | **Audit cohérence & dette** (3ᵉ audit post-phases) — doc 02 périmé (features CDC jamais construites : double honoraire AMO, raison_sociale, taux_1/2, artisan_documents) ; bug redevances réduit à 1 DROP (bonne contrainte déjà là) ; UNIQUE agences déjà posée ; objectifs_ca unicité confirmée ; hardcode prompt CR Martigues (nouveau) ; finance.js ~35 clés mortes recensées | ✅ lecture seule |
| 02/06 | **Bloc A — bug unicité redevances ✅** : DROP de l'ancienne contrainte `(mois, annee)`, la bonne `(agente_id, annee, mois)` existait déjà en index. Testé prod (2 agentes même mois OK). SQL versionné A1 | ✅ appliqué main |
| 02/06 | **Bloc A — nettoyage orphelins Storage** : 21 fichiers résiduels supprimés (3 dossiers + 2 artisans supprimés dont le ménage Storage avait été manqué ; 0 référence vivante vérifiée sur 7 tables). Buckets propres avant L12 | ✅ fait via interface Storage |
| 02/06 | **Chantier finances (B) — étape 1 : base honoraires corrigée.** Bug `finance.js:76` (`getSignedTotals` filtrait `commission_pourcentage > 0`, excluant à tort les devis 0% de la base honoraires, contraire doc 04 l.41). Filtre retiré (1 ligne). Prouvé sur 2026-AM-002 : honoraires 6 895,27 → 7 623,21 € ; commissions/apporteur inchangés ; dossier sans 0% non régressé. Branche `fix/honoraires-base-devis-0pct` | ✅ mergé |
| 02/06 | **Chantier finances (B) — finance.js complété : acomptes + périmètre devis.** (1) Acomptes artisans calculés dans finance.js (montant + mode + totalSignes/totalActifs), gestion native sentinel -1/0. (2) `getActiveDevis` corrigé en liste blanche (recu/accepte/a_modifier) → exclut en_attente du prévisionnel. (3) Définition UNIQUE de « signé » : `getSignedDevis` + flag `.signed` excluent a_modifier même avec date_signature (cas client qui revient sur un devis signé). Impact réel vérifié : Jadras (honoraires réels −15 298 € base, devis a_modifier sortis du réel, statut confirmé correct), en_attente 0 € d'impact. | ✅ mergé |
| 03/06 | **Finances (B) Étape 1 — Acomptes rebranchés** : 6 sites de chantiers/[id] → `calculateDevisFinance().acompte`, helper `montantAcompte` supprimé, `solde` ajouté à finance.js. Testé Guerteau | ✅ mergé |
| 03/06 | **Finances (B) Étape 2 — Filtres devis rebranchés** : 3 filtres → `getSignedDevis`/`getActiveDevis`, bug latent en_attente corrigé, effet nul (test 48 devis). Mergé `d9df59f` | ✅ mergé |
| 03/06 | **Audit zones 4-5-6-7** (rebranchement chantiers/[id]) : ZONE 7 persistance = argent réel (relu par honReel) mais déclenchée sur clic seulement ; 5+6+7 inséparables ; migration 10 lignes suivi_financier nécessaire ; décision ZONE 6 = standard 6/9 pur | ✅ lecture seule |
| 03/06 | **Finances (B) Étape 4 — Commission rebranchée** : 3 calculs de commission de chantiers/[id] (Commission prévue HT l.2087, par devis l.2668, échéance l.3288) → `calculateCommissionsFinance().comHT` / `calculateDevisFinance().comHT`. Affichage pur. Neutre (écart sub-centime d'arrondi). Réutilise finAc/finDv (DRY). Branche `feat/rebranch-commission-chantier` | ✅ mergé |
| 03/06 | **Finances (B) — préalables gros lot 5+6+7 terminés (audits + ménage data).** (1) Photo `suivi_financier` : AUCUNE migration de modèle nécessaire — les lignes persistées utilisent déjà les taux réels du dossier (le 0.09 hybride n'est jamais persisté, seulement affiché) et aucun dossier `frais_statut='rembourse'` n'a de ligne ; impact réel d'un rebranchement ≈ 5€ (Eppinger, ligne réglée intouchée). (2) Élucidation contradiction écran/base : la ligne frais s'affiche d'après `dossiers.frais_statut`, PAS `suivi_financier.statut_client`. (3) Ménage : 4 résidus de clics test Jadras supprimés (acompte_amo 310,50 périmé + honoraires_courtage/solde_amo/acompte_artisan L Habitat, tous NULL/en_attente). SQL versionné `CLEAN_jadras_residus_toggle.sql`, DELETE par id + rollback, contrôle avant/après OK. | ✅ appliqué main |
| 03/06 | **Finances (B) Étapes 5+6+7 — Honoraires + Récap + Persistance** (LE gros lot) : honoraires rebranchés sur finDossier (modèle frais finance), standard Récap = 6/9 pur (Eppinger 10635→11079), note « frais déduits » sur dossiers rembourse, base section sur finance, orphelins baseCourtageHTTC/fraisHT supprimés. ZONE 7 persistance VALIDÉE prod (Jadras : toggle persiste montant finance 7802,92). Mergé `ea73a41` | ✅ mergé |
| 03/06 | **Finances (B) — dashboard rebranché** : filtre devis l.129 (`!== refuse`) → `getActiveDevis`. Dashboard était déjà sur finance.js pour tous les montants (fin.*, dvFin.netCom) ; seul ce filtre restait en dur. Neutre (le seul en_attente a netCom=0 ET non débloqué). Branche `feat/rebranch-filtre-dashboard` | ✅ mergé |
| 03/06 | **Finances (B) — page finance, filtres devis** : l.254/255/628 → `getActiveDevis`/`getSignedDevis`. Neutre (ensembles identiques, test Jadras/Eppinger). LOT royalties `0.05/0.95` NON rebranché (volontaire) : opère au niveau agrégat mensuel, pas par devis ; reconstitution algébriquement EXACTE (écart 0) → pas un bug, swap exigerait de restructurer l'agrégation F2 (risque pour gain nul). Branche `feat/rebranch-finance-filtres-royalties` | ✅ mergé |
| 04/06 | **Finances (B) — finance.js : cœur honoraires paramétrable** : extraction de `honorairesCore(dossier, {totalHT, totalTTC})` (arithmétique commune signés/actifs/recu+accepte) + nouvelle fonction `calculateHonorairesRecuAccepte` (base recu+accepte pour le Recap PDF preview) + champ `courtage.brut`/`htBrut` + `standard.courtageTTCBrut` + export `COURTAGE_STANDARD`/`AMO_STANDARD`. STRICTEMENT ADDITIF : non-régression bit-à-bit prouvée (Eppinger/Cossanteli/Jadras/courtage simple, avant/après identiques). Prépare le rebranchement du Recap PDF. Branche `feat/finance-honoraires-base-parametrable` | ✅ mergé |
| 04/06 | **Finances (B) — Recap PDF mutualisé (composant partagé)** : création `app/lib/pdf/RecapHonoraires.js` (autonome, branché finance, maquette standard→remise→réel à 5 cas + virgules FR) ; branché dans route.js (Recap_Financier), restitution.js `buildContentPDF` (DossierSuivi final) et `buildR3ContentPDF` (R3 preview). Source unique : ~178 l de duplication supprimées (route −86, restitution −92). Remise courtage désormais visible (<6%), AMO simulé sur dossier courtage (si devis en négociation, taux 9% standard sans remise). R3 « TOTAL PROJET »→« TOTAL CHANTIER » par scénario. 5 cas + rembourse validés en PDF réels. Branche `feat/recap-honoraires-partage` | ✅ mergé |
| 04/06 | **Finances (B) — fix double comptage honoraires AMO** : en état AMO intermédiaire (acompte réglé/solde non), honReel + agrégation F2 additionnaient `courtNet + acompteAmoNet` alors que l'acompte AMO = la part courtage (même encaissement). Corrigé : acompte réglé → `courtage.net` seul ; +solde → `+soldeAmo.net` ; jamais les deux. Bloc acompte_amo F2 supprimé, `/1.1` + variables acompteAmo* supprimées. 2 dossiers corrigés (audit base, le doc disait 1) : AM-009/Stevan 1996→998, AM-001/agente 12762→5970 (part agente 6381→2985). Eppinger (réglé) + CT-005 (courtage) inchangés. CODE seul, aucune migration. Branche `fix/double-comptage-amo` | ✅ mergé |
| 04/06 | **Finances (B) — chargerTout robustesse** : `chargerTout` récupère désormais le `{ error }` de chaque requête (avant : ignoré → écran 0 € silencieux) ; requêtes critiques (dossiers/redevances/factures_agente/objectifs) → throw → bandeau d'erreur plein écran au lieu de chiffres faux ; optionnelles (agentes/admin) tolérantes ; `.catch` ajouté (corrige aussi un spinner infini sur exception réseau). Réutilise le mécanisme `erreur`/`setErreur` existant. Cas nominal strictement inchangé. Branche `fix/finances-chargertout-erreurs` | ✅ mergé |
| 04/06 | **Finances (B) — suppression `frais_deduits` (code)** : orphelin de l'ancien modèle base-réduction (remplacé par `frais_statut='rembourse'`). 13 références retirées (finance.js gain l.105, 8 Recap chantier, 3 PDF, case UI, clé update, SELECT dashboard, doc 04). Règle métier actée : frais remboursés = TOUJOURS un gain (encaissés), juste retirés du courtage. `frais_deduits=false` sur 25/25 dossiers → suppression strictement NEUTRE. Colonne SQL pas encore droppée (lot SQL séparé). Branche `refactor/suppr-frais-deduits-code` | ✅ mergé |
| 04/06 | **Finances (B) — fix date toggle honoraires + nettoyage** : le toggle de règlement (honoraires_courtage/acompte_amo/solde_amo) écrit `date_paiement=today` au coche et SUPPRIME la ligne au décoche (DELETE) ; affichage du toggle montre `date_paiement` quand réglé (au lieu de l'échéance prévue figée). Corrige règlement rangé au mauvais mois F1/F2 (Jadras : honoraire de juin tombait en janvier via fallback date_signature_contrat) + résidus de décochage. Gate strict (statut_client + types honoraires) → apporteur/illiCO/artisan/factures inchangés. Effet de bord AMO hérité. PR #116 (base/F2 `c0e2fc3` + affichage). Nettoyage data : DELETE résidu AM-009 solde_amo + UPDATE AM-002 solde_amo date 30/04 (activité avril, facturée mai via décalage F1). SQL versionné | ✅ mergé |
| 05/06 | **Finances (B) — désynchro frais (synchro data)** : 3 lignes frais_consultation (AM-006/AM-007/CT-004) en statut_client='en_attente' alors que dossier frais_statut='regle' → PDF "non réglé" à tort. UPDATE statut_client→'regle' par id (dates réelles conservées). Résidu legacy. Fichier `docs/sql/SYNC_frais_consultation_statut.sql` | ✅ appliqué |
| 05/06 | **Finances (B) — frais : source unique du statut** : statut « frais payé » lu depuis dossiers.frais_statut partout (vraie source) ; ligne frais_consultation réservée à date_paiement. Corrige restitution.js (paye=frais_statut==='regle') + finances.js:601. Ferme les 2 trous double-source (désync inerte + dossier sans ligne → "payé" correct). Cause racine fermée. Branche `fix/frais-statut-source-unique` | ✅ mergé |
| 05/06 | **Finances — hygiène zéro-hardcode (chiffre-neutre)** : remplacement des constantes financières en dur par les constantes finance.js (COURTAGE_STANDARD, AMO_STANDARD, TVA, ROYALTIES_RATE — TVA+ROYALTIES_RATE passées en export). 12 remplacements (chantiers/[id] ×9 + finances ×3) + bonus interne finance.js (getTauxCourtage/getTauxAmo). Équivalence prouvée bit-à-bit en node (incl. conversions ×100 et 0.95=1-RR). Garanti chiffre-neutre. NON touchés : ×1.1 prefill (taux travaux 10%) et fallback TVA ×1.2 (lot suivant). Branche `refactor/hygiene-constantes-finance` | ✅ mergé |
| 05/06 | **Finances — TVA travaux vs frais** : constante `TVA=1.2` générique séparée en `TVA_FRAIS=1.2` (frais consultation 20%) et `TVA_TRAVAUX=1.1` (devis travaux 10%). Les 4 fallbacks reconstituant le TTC d'un devis sans montant_ttc passent à 10% (étaient à 20% à tort) ; frais inchangés. comTTC (clé morte) → TVA_FRAIS (suppression au lot code mort). Chiffre-neutre sur l'actif (0 devis actif sans TTC : seul AM-006, en_attente+HT null, inerte) ; corrige le comportement futur. Branche `fix/tva-travaux-vs-frais` | ✅ mergé |
| 05/06 | **DROP `factures_agente_backup_b7b`** : table backup orpheline supprimée. Auditée lecture seule (0 usage code, 0 dépendance entrante FK/policy/trigger/vue/routine, clone structurel figé 8 lignes vs 20 vivantes, snapshot migration ~03/05). SQL versionné `docs/sql/DROP_factures_agente_backup_b7b.sql` (archive des 8 lignes en commentaire avant DROP). Contrôle avant/après : backup absente, `factures_agente` vivante intacte (20 lignes). | ✅ appliqué (9f5af46) |
| 05/06 | **`partenaire_taux` sur l'artisan — choix de modèle acté** : pas de taux commission partenaire stocké sur la fiche artisan ; la commission est décidée AU DEVIS (`devis_artisans.commission_pourcentage`), variable par devis. Intentionnel, pas un manque. Aucune action code. | ✅ acté |
| 05/06 | **Renommage `isMarine`/`estChantierMarine` → `isAdmin`/`referentEstAdmin`** : variables mal nommées (logique déjà role-based `role==='admin'`, multi-admin-safe, aucun ID Marine en dur). Pur renommage sur 4 fichiers (clients, chantiers, finances, chantiers/[id], ~32 occurrences) + suppression de 2 `const isAdmin` redondants (finances l.185 closure, chantiers/[id] l.3277 doublon de referentEstAdmin). 0 orphelin prod, `_design` intact, comportement inchangé. Branche `refactor/rename-ismarine-isadmin` | ✅ mergé (456f1d4) |
| 05/06 | **Fallback `\|\| 'Marine'` en dur → `\|\| 'Moi'`** : sélecteur de périmètre finances (option `key:'moi'`, admin-only, 2 sites). Le fallback affichait « Marine » en dur si un admin n'a pas de prénom (faux pour un 2e admin). Remplacé par « Moi » générique (cohérent avec key:'moi', jamais faux). Dernier nom de franchisée hardcodé en prod supprimé (grep 'Marine' hors _design = 0). Branche `fix/fallback-marine-en-dur` | ✅ mergé (1511ecd) |
| 05/06 | **L19 bug molette inputs number** : scroller la molette sur un `<input type="number">` focalisé changeait sa valeur (natif), dangereux sur les champs financiers (500→499,98 en base). Fix global : composant client `DisableNumberInputScroll` monté dans layout.js → listener `wheel` passif document-level qui blur l'input number focalisé survolé (garde-fou `el===e.target`, cleanup removeEventListener). Couvre les 17 inputs actuels + futurs sans les toucher. Clavier/flèches ↑↓/focus intacts. Branche `fix/l19-molette-input-number` | ✅ mergé |
| 05/06 | **Upload devis — référence pendante (vrai bug data)** : les 2 sites d'upload PDF devis (chantiers/[id] ajout rapide + modale) écrivaient `devis_pdf_path` inconditionnellement, même si l'upload Storage échouait → base pointant vers un fichier inexistant, « Voir devis » cassé. Corrigé : capture de `uploadError`, écriture du chemin gatée sur le succès, `setErreur` informatif en cas d'échec (devis créé, PDF à re-uploader). Cas nominal inchangé. Branche `fix/upload-devis-reference-pendante` | ✅ mergé |
| 05/06 | **Code mort `d.objet` + purge Storage CR** : (A) retrait du bloc `{d.objet && ...}` (clients/[id], colonne `objet` inexistante → jamais rendu, info couverte par `description`) ; (B) ajout de la purge `chantiers/{id}/cr/` à la suppression de chantier (les PDF de CR restaient orphelins dans Storage, seul préfixe non purgé). Même pattern `removeFolderContents` que les purges voisines. Branche `fix/code-mort-objet-purge-cr` | ✅ mergé |
| 05/06 | **Feedback uploads silencieux (C2)** : 6 sites d'upload affichaient un succès même en cas d'échec (ou ne disaient rien) → corrigé par gate-succès (flag) pour éviter l'overwrite du setErreur par un setSucces, + succès partiel sur les boucles photo/document (« X ajoutées, N en échec »). 1 site simple (PDF facture) → else setErreur. Gate du chemin inchangé (0 référence pendante). 9 sites déjà OK non touchés. Branche `fix/upload-feedback-erreur` | ✅ mergé |
| 05/06 | **Trigger `set_dossier_referente_from_client` — harmonisé | ✅ mergé |
| 05/06 | **Archivage clients — Lot 1 : schéma + trigger propagation** : colonnes `archive boolean NOT NULL DEFAULT false` sur `clients` et `dossiers` ; fonction `propagate_client_archive()` (SECURITY DEFINER, search_path public) + trigger `clients_propagate_archive` (AFTER UPDATE OF archive, WHEN old IS DISTINCT FROM new) propageant clients→dossiers dans les deux sens. RLS vérifiée (clients_scope/dossiers_scope en cmd=ALL cloisonnent l'UPDATE ; trigger DEFINER nécessaire pour propager les dossiers d'autres référentes du même client, borné client_id). Anti-récursion confirmée. Testé en base sur 2026-AM-028 (archive→dossier suit ; désarchive→revient). SQL versionné `ADD_archive_clients_dossiers.sql`. | ✅ appliqué + commit et mergé f065a47 |
| 05/06 | **Archivage clients — Lot 2a : helper `app/lib/clients.js`** : 3 fonctions pures (archiverClient/desarchiverClient = UPDATE clients.archive, propagation laissée au trigger DB ; supprimerClient = DELETE avec mapping FK 23503 → message « ce client a des dossiers, archivez-le plutôt »). Helper pur (supabase + id injectés, aucune navigation/état). Garde-fou suppression = FK existante, pas de comptage applicatif. Appelé par l'UI au Lot 4. Branche `feat/archivage-clients-lot2-helper` | ✅ mergé |
| 05/06 | **Archivage clients — Lot 2b : garde-fou création dossier** : `chantiers/nouveau` bloque la création pour un client archivé. Deux niveaux : masquage du formulaire au chargement (panneau « Client archivé » + retour fiche) si `client?.archive`, et re-test défensif `if (client?.archive) return` en tête de handleSubmit. `client.archive` lu du `select('*')` existant (0 requête ajoutée). Applicatif seul (pas de trigger DB), chokepoint unique = dernier rempart même sur URL directe. Non-régression : client non archivé inchangé. Branche `feat/archivage-clients-lot2-gardefou-creation` | ✅ mergé |
| 05/06 | **Archivage clients — Lot 3a+3c : masquage lectures opérationnelles** : `.eq('archive', false)` sur liste clients (`clients/page.js`), sélecteur (`ModaleChoixClient`), liste chantiers (`chantiers/page.js`), barres planning. + filtrage mémoire (3c) sur RDV/interventions du planning : `archive` ajouté aux joins, retrait `dossier?.archive !== true` ⚠️ négation stricte → RDV sans dossier (prospect) CONSERVÉS. Finances/dashboard non touchés (compta intacte ; dashboard = 3b). Branche `feat/archivage-clients-lot3-masquage` | ✅ 9065f06|
| 05/06 | **Archivage clients — Lot 3b : dashboard masquage fin** : `archive` ajouté au select dashboard (PAS de `.eq` → CA non troué) ; `dossiersActifs = dossiers.filter(d => d.archive !== true)` dérivé en mémoire pour les listes affichées (enCours, aRelancer, Pipeline, activité récente, sous-compteur) ; `computeCAMensuel(dossiers)` reste sur TOUS les dossiers (CA/compta préservés) ; RDV du jour filtrés `!== true` (sans dossier conservés). Testé : listes masquées + CA inchangé. Branche `feat/archivage-clients-lot3-masquage` (3a+3c+3b) | ✅ bd6377f  |
| 05/06 | **Archivage clients — Lot 4a : actions fiche client** : bouton conditionnel sur la fiche (`clients/[id]`) — Archiver (`btn-ghost`) si dossiers.length>0, Supprimer (rouge `var(--bad)`) si =0. confirm() natif (pattern maison) → helper Lot 2 → succès `router.push('/clients')` / erreur `setErreur`. Icônes Archive/Trash ajoutées. Branche `feat/archivage-clients-lot4-actions` | ✅ commit a1d3904  |
| 05/06 | **Archivage clients — Lot 4b : actions menu ⋮ liste** : 2 entrées Archiver (hide si 0 dossier) / Supprimer (hide si ≥1) dans le tableau d'actions du menu (`clients/page.js`). confirm() natif → helper Lot 2 → erreur alert() / succès retrait local `setClients(filter)`. Fermeture menu via wrapper existant (pas de doublon). Filtre archive Lot 3 intact. Branche `feat/archivage-clients-lot4-actions` (4a+4b) | ✅ commit 25026a8 |
| 05/06 | **Archivage clients — Lot 4c : vue archivés + désarchiver** : toggle « Actifs/Archivés » SÉPARÉ de ongletsList (visible tous rôles, sinon agentes ne verraient pas leurs archivés) ; requête paramétrée `.eq('archive', vueArchives)` + dep useEffect (recharge au toggle) ; entrée « Désarchiver » au menu ⋮ (hide !vueArchives) → desarchiverClient → retrait local. Cloisonnement agente (`.eq('referente')`) préservé en vue archivés. Branche `feat/archivage-clients-lot4c-vue-archives` | ✅ commit dbeaeaa |
| 05/06 | **Archivage clients — Lot 4d : bandeau fiche client archivé** : bandeau « Client archivé » conditionnel sur `clients/[id]` ; bouton à 3 cas exclusifs (archivé→Désarchiver / actif+dossiers→Archiver / actif+0→Supprimer) ; « Nouveau dossier » masqué si archivé (cohérent garde-fou Lot 2b). Désarchiver = confirm→helper→maj locale optimiste `setClient({...archive:false})` (déterministe, fiche redevient active sur place). Allègement docs ÉCARTÉ (gain marginal, non prioritaire). Branche `feat/archivage-clients-lot4d-bandeau` | 🔄 commit en attente |
| 05/06 | **Code mort finance.js — pilote : suppression `comTTC`** : clé morte (confirmée audit TVA + recensement 3 axes, axe sérialisation vide) retirée aux 4 sites : calcul + return dans `calculateDevisFinance`, agrégat reduce + return dans `calculateCommissionsFinance`. Couple traité ensemble (devis alimentait l'agrégat). comHT/royaltiesType2/netCom intactes. TVA_FRAIS conservée (vivante l.104/210 + chantiers/[id]). Équivalence des clés restantes prouvée (node). grep comTTC = 0 hors _design. Branche `refactor/finance-suppr-comTTC` | merge 8c74364 |
| 05/06 | **Code mort finance.js — groupe 1 : doublons de provenance** : 7 clés de return mortes retirées (devis `partenaire`/`paiementDirect` ; honoraires + settings `typologie`/`tauxCourtage`/`tauxAmo`) — doublons exposés de champs source (`dossier.*`, `artisan.*`, `getTaux*`), jamais relus. ⚠️ Variables locales homonymes de `honorairesCore` CONSERVÉES (vivantes : isCourtage/brut/ttc). `isApporteur` conservé (jumeau vivant de paiementDirect). Équivalence prouvée (node). Branche `refactor/finance-suppr-doublons-provenance` |commit a02f6a9 |
| 05/06 | **Code mort finance.js — groupe 2 : agrégats morts (dernier lot 🟢)** : 21 clés retirées (devis.montantHT, frais.fraisTTC, honoraires.{totalTTC,totalHT,signedDevisCount,totalDevisHTSignes,parts,courtage.htBrut}, honorairesPrevi.{totalTTC,totalDevisHTRecus}, royalties.{commissions,frais,honoraires}, gains.{bruts,nets,brutsPrevi}, acomptes, settings.partAdmin, apporteur.{tauxDefini,totalHTReel}) + cascades d'orphelins (gainsBruts/Nets, core.totalHT, signed, etc.). honoraires.parts retirée par cascade (validé) — asymétrie avec honorairesPrevi.parts TRACÉE par commentaire. Locaux vivants gardés (royaltiesCommissions/Frais/Honoraires→total, gainsBrutsPrevi→netsPrevi). getPartAdmin export conservé. lines[].totalHTReel (🟠) intact. Équivalence prouvée (node). Branche `refactor/finance-suppr-agregats-morts` | commit c717ded  |
| 06/06 | **Code mort finance.js — 🟠 : 4 sous-clés lines[] retirées (CLÔT le sujet)** : `apporteur.lines[].{artisanId, tauxApporteur, debloque, totalHTReel}` retirées des 2 constructions de ligne (modes total_chantier_ht + par_devis) — doublons recalculés ailleurs (artId via dv.artisan_id, taux top-level, paye via suivi_financier, agrégat totalHTReel retiré hier). ⚠️ Variables locales homonymes CONSERVÉES (vivantes : estDebloque, agenteReel/adminReel, partsReel). adminReel intact (agrégat l.407→partsReel.admin). Équivalence prouvée 2 modes (node). 3 clés génériques (statut/montantTTC/actif) gardées volontairement (API latente). Branche `refactor/finance-suppr-lines-mortes` | f0f7a0b |
| 06/06 | **L12 Storage — Lot 2 : client_read_photos cloisonné (faille externe FERMÉE)** : la policy autorisait tout client (role='client') à lire n'importe quelle photo → remplacée par vérif d'appartenance (`profiles.client_id = dossiers.client_id`, dossier extrait du chemin via split_part). Migration NON nécessaire (chemin actuel `chantiers/{dossierId}/` suffit). Testé en base TRUE (ses photos) / FALSE (autre client) / FALSE (staff exclu). SQL versionné `L12_client_read_photos_cloisonne.sql`. Reste : Lot 1 (lecture staff), Lot 3 (écritures). Branche `fix/l12-storage-client-photos` | 1540674 |
| 06/06 | **L12 Storage — Lot 1 : lecture staff cloisonnée (faille interne FERMÉE)** : « Lecture documents » + « Lecture photos » (role-only) remplacées d'un bloc par des policies à branches par préfixe gardées par foldername[1] (fail-safe). chantiers→miroir exact dossiers_scope (admin société / agente ses dossiers), artisans→société, factures_agente/kbis/rib→owner ou admin société (strip extension via double split_part). Placeholder autorisé staff. Testé en base 9 cas TRUE/FALSE sur tous préfixes + 2 sens + non-régression client_read_photos. génération PDF (service_role) vérifiée OK après merge. SQL versionné `L12_lecture_staff_cloisonnee.sql`. Reste : Lot 3 (écritures). Branche `fix/l12-storage-staff-lecture` | b47ea56 |
| 06/06 | **L12 Storage — Lot 3 : écritures staff cloisonnées (L12 CLOS)** : 5 policies d'écriture (Upload/Suppression documents+photos, Remplacement factures agente) remplacées d'un bloc — mêmes prédicats par préfixe que le Lot 1, appliqués à WITH CHECK (INSERT) / USING (DELETE) / les deux (UPDATE). Resserrement UPDATE factures_agente role-only → owner/admin société. Périmètre strict α (pas d'élargissement kbis/rib → lot suivant). Testé en base 9 cas (3 commandes × 2 sens). SQL versionné `L12_ecritures_cloisonnees.sql`. Branche `fix/l12-storage-ecritures` | 4e89e96 |
| 06/06 | **Storage fichiers perso — kbis admin-only, rib remplaçable** : modèle de droits affiné sur 2 policies (INSERT « Upload documents » + UPDATE « Remplacement factures agente »). kbis = admin-only (dépôt ET remplacement — pièce officielle gérée par l'admin, cohérent avec parametres admin-only) ; rib + factures_agente = owner/admin. Corrige au passage le remplacement rib (aucune policy UPDATE ne le couvrait → upsert échouait en silence). Branches chantiers/artisans inchangées. Testé en base 10 cas (3 préfixes × INSERT/UPDATE × 2 sens). SQL versionné `storage_kbis_admin_only.sql`. Branche `fix/storage-kbis-admin-only` | 5bbf021 |
| 06/06 | **Feature : mot de passe oublié** : mode `forgot` de login câblé (était inerte) — input contrôlé + `resetPasswordForEmail(email, {redirectTo: .../auth/set-password?type=recovery})` + message anti-énumération générique. set-password lit `?type` pour le wording (recovery vs invite), logique session→updateUser inchangée, rétrocompat invitation (absence de type = invite, create-agente non touché). Suspense autour de useSearchParams. ⚠️ vérifier template email Reset Password côté Supabase. Branche `feat/mot-de-passe-oublie` | 81ddf44 |
| 06/06 | **Fix : devis signé dans le PDF de suivi** : `buildDossierSuivi` embarquait le fichier devis selon le stade du dossier (isPreSignature) → non-signé embarqué même pour un devis signé (pré), rien si pas de scan (post). Corrigé : boucle unique per-devis sur recu+accepte, `devis_signe_path || devis_pdf_path` (signé prioritaire, fallback non-signé silencieux). Ensemble post-signature élargi à recu+accepte. Factures + données (montants/statut) inchangés. a_modifier/refuse/en_attente exclus. Branche `fix/pdf-suivi-devis-signe` | 002feca |
| 06/06 | **Profiles — verrou champs admin-managed (prérequis page profil agente)** : `profiles_protege_identite` étendu. Une agente ne peut plus s'auto-modifier part_agente_defaut, frais_part_agente_defaut, parts_agente_disponibles, redevance_*, agence_id, kbis_url (garde `get_my_role() <> 'admin'`). Faille latente (RLS profiles_update valide la ligne, pas les colonnes) fermée AVANT la page profil qui l'aurait exposée. role/societe_id toujours verrouillés pour tous. Admin + service_role (onboarding) préservés. Testé : agente part BLOQUÉE / agente tél OK / admin OK / système OK. Branche `fix/profiles-verrou-champs-admin` | ✅ 8c24c49 |
| 06/06 | **Feature : page profil agente (`/profil`)** : page agente-only, layout 4 blocs (Informations / Rémunération / Mes documents / Mot de passe). Tél éditable ; nom/prénom/email/agence/rôle lecture seule ; rémunération lecture seule (paliers de commission depuis parts_agente_disponibles + frais + redevance ; fusion en 1 ligne si 1 seul palier == frais, sinon sections séparées) ; RIB owner (voir/remplacer) ; Kbis lecture seule (admin-only) ; mdp (updateUser). Item navbar agenteOnly. Handlers réutilisés de parametres (non touché). Sécurité : verrou DB (8c24c49) bloque toute modif forgée des champs rému. Branche `feat/page-profil-agente` | ✅ 524b4f5 |
| 06/06 | **L14b Lot 1 — UNIQUE (societe_id, code) sur agences** : contrainte explicite `agences_societe_code_unique` posée (prérequis génération code agence). ⚠️ Découvert au test : un index unique préexistant `agences_code_par_societe_uniq` couvrait déjà le couple (non vu à l'audit qui ne regardait que pg_constraint, pas pg_indexes) → doublon nettoyé (DROP de l'index nu préexistant, on garde la contrainte). Base = une seule règle d'unicité. Testé (rejet doublon par agences_societe_code_unique). Branche `feat/l14b-unique-agence-code` | ✅ fb509d3 |
| 06/06 | **L14b Lot 2 — Génération auto du code agence (format LLNN)** : trigger BEFORE INSERT `agences_generer_code` génère `code` si non fourni = 2 lettres ville normalisée (unaccent+upper+[A-Z]) + 2 chiffres (compteur par société+préfixe, départ 00). Padding X si ville <2 lettres ; exception si >99 ; code fourni respecté. Migration legacy MAR→MA00 appliquée avant (base 100% LLNN). Extension unaccent installée (schéma extensions). REVOKE PUBLIC/anon (cohérence durcissement). Contrainte UNIQUE Lot 1 = filet. Testé 7 cas (IS00/IS01/MA01/EV00/AI00/ZZ99/YX00). Branche `feat/l14b-code-agence` | ✅ b818808 |
| 07/06 | **L14b — Validation domaine email staff (@illico-travaux.com) à l'invitation** : helper `app/lib/email-validation.js` (`isAllowedStaffEmail` : domaine illico-travaux.com en constante + exceptions via env `STAFF_EMAIL_EXCEPTIONS`, normalisation lowercase+trim, borne @ anti-contournement préfixe/sous-domaine). Branché CÔTÉ SERVEUR dans create-agente (refus 400 avant inviteUserByEmail). Login NON touché (comptes test live.fr/outlook.com préservés), clients non concernés. Helper réutilisable pour l'onboarding admin. .env.example créé. ⚠️ STAFF_EMAIL_EXCEPTIONS à définir dans Vercel. Branche `feat/validation-domaine-staff` | ✅ 87d4a41 |
| 07/06 | **L14b 3a (partie 1) — Table `admin_invitations` (service_role-only)** : 1ère brique onboarding. Enregistre « email autorisé à créer une société ». Colonnes : email (normalisé route), user_id (FK auth.users CASCADE), statut (en_attente/consommee/revoquee), created_at/consumed_at/invited_by/expires_at. Index unique partiel `(email) WHERE statut='en_attente'` (anti-doublon, ré-invitation possible après révocation/consommation). RLS activée + 0 policy + REVOKE anon/authenticated = strictement service_role-only. Testé : T1 permission denied lecture+écriture authenticated (sécurité centrale), T2 doublon rejeté / en_attente+consommee OK, T3 CHECK. Branche `feat/3a-table-admin-invitations` `ded4587` | ✅ 21d11fa |
| 07/06 | **L14b 3a (partie 2) — Route `/api/admin/invite-franchise`** : route protégée par secret (ADMIN_INVITE_SECRET, pattern CRON_SECRET) que seule Anne-Lise appelle pour inviter un admin franchisé. Valide domaine (helper isAllowedStaffEmail) → anti-doublon (409 si en_attente/consommee, revoquee n'empêche pas) → check déjà-admin (profiles.email) → inviteUserByEmail(role:admin) → INSERT admin_invitations (user_id, expires_at +7j) → rollback deleteUser si insert KO. redirectTo set-password PROVISOIRE (rebrancher 3c). Testé preview : 401 faux secret / 200 + mail + ligne en_attente / 409 ré-appel / 400 hors domaine. Branche `feat/3a-route-invite` `c7dc4e7` | ✅ 239be5c |
| 07/06 | **L14b 3b — Garde onboarding (connecté sans profil)** : `profileStatus` (loading/loaded/absent) ajouté à AuthProvider via `.maybeSingle()` (erreur réseau → reste loading, jamais « absent » par erreur ; initialized inchangé). Composant `OnboardingGuard` centralisé (layout, sous AuthProvider) : user sans profil → /onboarding (sauf routes EXEMPT) ; user avec profil sur /onboarding → dashboard ; loading → no-op (anti-flash/boucle). Coquille `/onboarding` placeholder (formulaire = 3c). Synergie 3a : l'admin invité (redirectTo provisoire → dashboard) est basculé vers /onboarding par le guard. Testé : non-régression compte avec profil OK + epfedu sans profil → /onboarding. Branche `feat/3b-onboarding-guard` `a26896b` | ✅ 83b8795 |
| 07/06 | **L14b 3c (partie 1) — Fonction `onboarding_create_societe` (atomique)** : fonction Postgres SECURITY DEFINER, cœur de l'onboarding. Dans 1 transaction : vérifie invitation (user_id=p_user_id, en_attente, non expirée, FOR UPDATE) → garde anti-double profil → crée société → agence (code LLNN auto via trigger) → profil admin (agence_id NULL, email depuis auth.users) → consomme invitation. Option A : p_user_id en paramètre (route vérifie JWT, passe id de confiance), REVOKE PUBLIC/anon/authenticated + GRANT service_role seul. Codes RAISE courts (INVITATION_INVALIDE/PROFIL_EXISTANT/USER_INTROUVABLE) traduits par la route. Testé (transactions annulées) : T1 succès 4 entités + code IS00 + consommee / T2-T3 INVITATION_INVALIDE / T4 PROFIL_EXISTANT / invitation epfedu préservée en_attente. ⚠️ SQL editor Supabase tourne en authenticated par défaut → RESET ROLE en tête de chaque bloc privilégié. Branche `feat/3c-fonction-create-societe` `364d021` | ✅ 10303a2 |
| 07/06 | **L14b 3c (partie 2) — Route `/api/onboarding/create-societe`** : relaie le formulaire onboarding vers la fonction atomique. Auth JWT LÉGÈRE inline (`getUser(token)` sans lookup profil — requireUser ferait 403 car user sans profil) → user.id. p_user_id depuis le JWT vérifié (non falsifiable, pas dans le body). Validation champs requis (nom_societe/agence_nom/agence_ville/nom/prenom → 400). Appel `rpc('onboarding_create_societe')` en service_role, optionnels en '' (fonction fait nullif). Traduction codes : INVITATION_INVALIDE→403, PROFIL_EXISTANT→409, USER_INTROUVABLE→400, autre→500 loggé. api-auth.js intact. Branche `feat/3c-route-create-societe` `2746ac1` | ✅ 5e96fa1 |
| 07/06 | **L14b 3c (partie 3) — Formulaire onboarding + PARCOURS COMPLET VALIDÉ** : formulaire mono-page 3 sections (Société/Agence/Vous) dans /onboarding, classes design system (card/input/btn), email lecture seule, code agence non demandé (auto). Appel route via authHeaders (Bearer), succès → fetchProfile → OnboardingGuard bascule dashboard (fluide, pas de flash). Branche `feat/3c-formulaire-onboarding` `9bef494`, mergé `e8c6d13`. **TEST E2E réel (compte epfedu via Add user, rate limit email Supabase contourné)** : /onboarding OK → formulaire → société « TEST 1 » + agence « illiCO TEST » ville MARSEILLE code MA00 auto + profil admin agence_id NULL + invitation consommee, tout même created_at (transaction atomique). **Cloisonnement vérifié : depuis la nouvelle société, données CTP invisibles.** Onboarding L14b complet. | ✅ e8c6d13 |
| 07/06 | **L14b — Placeholders onboarding génériques** : remplacé les placeholders exposant les vraies infos CTP (CONSEIL TRAVAUX PROVENCE, illiCO travaux Martigues, Martigues, 13500, 948 096 888) par du générique (« Nom de votre société », « illiCO travaux [ville] », « Votre ville »...). Écran destiné aux nouveaux franchisés → pas d'info CTP. `fix/onboarding-placeholders-generiques` `1d0251e`, mergé `f835762`. **3d (page stats plateforme) reportée au backlog** (pas utile tant qu'un seul franchisé réel ; compteurs visibles via dashboard Supabase). | ✅f835762 |
| 07/06 | **L14b — Test de cloisonnement multi-société : VALIDÉ (étanche bidirectionnel)** : 2 sociétés réelles (CTP ef2128ea, 25 clients/26 dossiers/53 artisans/410 objets Storage ; TEST 1 65fad1cc, admin epfedu, 0 donnée métier). RLS simulée en base (transactions annulées) dans les 2 sens : CTP voit ses données + 0 fuite TEST 1 ; TEST 1 voit 0 donnée CTP (juste son agence + profil). 22 tables vérifiées dont tables FILLES (devis/finances/photos/messages/RDV/fiches via EXISTS dossiers/artisans) → 0 partout. Recensement exhaustif des tables cloisonnées documenté. Verdict : cloisonnement total prouvé au niveau RLS. **Backlog** : (1) Storage placeholder `factures_agente/.emptyFolderPlaceholder` visible cross-société = hygiène, PAS une fuite (0 octet, aucune donnée) ; (2) `specialites` non société-scopé = design VOULU (référentiel métiers commun, à cloisonner seulement si données par-société un jour). Volet UI à faire au fil de l'eau. | ✅ |
| 07/06 | **Multi-agence Lot 1 — Création d'agence** : route `/api/create-agence` (service_role, requireRole admin, societe_id du JWT jamais du body) + modale paramètres + `chargerAgence` `.single()`→liste `order('code')` + affichage liste (société 1×, N sous-blocs agence) + objectif d'agence masqué si ≥2 agences. code LLNN auto (trigger), non affiché. Sécurité societe_id prouvée par lecture du code. Testé : non-régression 1 agence, création 2e agence (TEST 1 : MO00 Montpellier), 403 agente. | ✅ 0ec94eb |
| 07/06 | **Multi-agence Lot 2 — Vue active + navbar bi-zone** : AuthContext étendu (`agences`/`agenceActive`/`setAgenceActive`). `agenceActive` null=Consolidé (agente/admin mono/admin multi défaut), uuid si admin multi clique (filtre d'affichage, pas rattachement). Persistance localStorage namespacée `batilis.agenceActive.<uid>`, validée contre la liste au montage (pas de race), purgée au logout. Navbar bi-zone (onglets « Toutes les agences » + par agence) si `admin && agences>=2` + déployée ; clic change l'état sans naviguer ; sous-titre reflète la vue active. Aucun écran scopé (Lot 4). Testé : non-régression CTP, bascule/persistance F5/validation valeur bidon sur TEST 1, agente sans onglet. | ✅ c05f9c6 |
| 08/06 | **Multi-agence Lot 3a — Sélecteur d'agence (create-agente)** : route double comportement (agence_id du body validé `.eq(societe_id)` du JWT → 400 si étrangère, AVANT l'invite ; absent → déduction `.order('code')`), agence résolue → profil + objectifs_ca. Modale `<select>` si `agences>=2` (source useAuth), défaut `agenceActive` modifiable, choix forcé si Consolidée. `refreshAgences()` ajouté à AuthContext (liste seule, pas `agenceActive`), appelé après création d'agence. Garde-fou CTP inchangé. Testé : non-régression CTP, TEST 1 ciblé, pré-remplissage, refreshAgences sans reload, sécurité agence étrangère (lecture code). | ✅ f863076 |
| 08/06 | **Multi-agence Lot 3b — Sélecteur d'agence (clients/nouveau)** : branche référente=admin → `<select>` si ≥2 agences (source useAuth, défaut agenceActive, choix forcé Consolidée) sinon déduction `.order('code')` ; référente=agente → agence de l'agente (inchangé). Insert client sous RLS : double barrière préexistante (clients_scope + trigger derive_societe_id) + validation côté client. Garde-fou CTP inchangé. | ✅ 9d866af |
| 08/06 | **Multi-agence Lot 4a — Scoping dashboard/clients/chantiers** : pattern `useMemo` (agenceActive null=tout, uuid=filter agence_id direct), source unique des dérivés (listes + CA + compteurs). `agence_id` ajouté au select dashboard. Dashboard reste « mes dossiers » (#8 voulu) + scopé agence ; widget RDV du jour renvoyé au lot Planning. Garde-fou CTP/agente inchangé. Testé : non-régression, bascule sans reload, cohérence agrégats. | ✅ 85ce8a2 |
| 08/06 | **Multi-agence Lot 4b — Scoping planning + widget RDV dashboard** : RDV/interventions filtrés via dossier parent (matchAgence, transverse !dossier_id toujours visible) ; jalons chantier + stat + dropdowns via dossiersScoped (agence_id direct) ; widget RDV du jour dashboard idem. Testé (transverse, intersection référente, non-régression). | ✅ 579d7d0 |
| 08/06 | **Multi-agence Lot 4b — Scoping planning + widget RDV dashboard** : RDV/interventions filtrés via dossier parent (matchAgence, transverse !dossier_id toujours visible) ; jalons chantier + stat + dropdowns via dossiersScoped (agence_id direct) ; widget RDV du jour dashboard idem. Testé (transverse, intersection référente, non-régression). | ✅ 579d7d0 |
| 09/06 | **Multi-agence Lot 4c-1 — Objectif d'agence scopé** : `getObjectif('agence')` sensible à `agenceActive` (uuid→agence, null→somme société), branche agente intacte. Migration `docs/sql/l4c1_fix_index_objectif_agence.sql` : index `objectifs_ca_agence_unique` `(annee,cible)`→`(annee,cible,agence_id)` — corrige bug multi-tenant (ancien index bloquait tout 2e franchisé). Testé : valeurs par agence, somme Consolidé 350k, agence sans objectif=0, non-régression CTP. | ✅ aff72fd |
| 09/06 | **Multi-agence Lot 4c-2 — Scoping redevances KPI** : `redevancesScoped` (useMemo) dans `totalNetCTP` (agente→mesRedevances, admin Consolidé→toutes, admin vue agence→r.agence_id). Corrige B1 ; pctObjectif cohérent (4c-1+4c-2). Suivi non touché. Validé non-régression CTP + code. | ✅ 87871e3 |
| 09/06 | **Multi-agence Lot 4c-3 — Périmètre unique finances** : scopedDossiers filtre agenceActive en amont du pill (corrige dette 4c-2, produits scopés agence) ; redevancesScoped même double filtre (4 cas) ; pill filtré par agence + réinit 'tous' au changement de vue. Modèle : navbar = seul sélecteur d'agence ; périmètre unique pilote KPI/F1-F2/Suivi/Synthèse. Validé non-régression CTP + bascule TEST 1 + lecture code. | ✅ b25b0c4 |
| 09/06 | **Multi-agence Lot 4c-4 — Scoping Suivi** : 5 usages dossiers→scopedDossiers (dont reduce apporteur) + 8 redevances→redevancesScoped, remplacements explicites (alias rejeté). Grep complétude 0 nu restant. Royalties inline suivent auto. Helper libellePerimetre + badge « Filtré sur ». Validé non-régression CR CTP (mois+année). | ✅ 85b6143 |
| 09/06 | **Multi-agence Lot 4c-5 — Scoping Synthèse** : reelData repointé rowsReelAnneeEnCours→rowsReelScoped (équivalence même agrégat), global mort supprimé ; previData/donut déjà scopés ; badge indicateur. Synthèse périmètre unique (B4 fermé). Validé non-régression CTP. → **LOT 4 COMPLET.** | ✅ 6ff525f |
| 09/06 | **Bloc A — Validation finances multi-agence (vraies données)** : agentes + redevances réparties sur TEST 1 ; scoping prouvé (Consolidé 5800 / Marseille 4500 / Montpellier 1300), objectif par agence, badge OK. + Audit 38 index UNIQUE = clean (rien à corriger, objectifs_ca déjà fait au 4c-1). → Bloc A sécurité levé. | ✅ |
| 10/06 | **Hardcodes sous-lot 2 mergé** : (branche feat/hardcode2-libelles-societe, depuis 402d8e7) : 20 libellés de rôle « CTP » → « Société » (finances/paramètres/chantiers). Identifiants techniques intacts, grep classé vide en résiduel. Écart d'audit constaté : chantiers n'avait qu'1 `gainAdmin` affiché (l'audit en annonçait 2) — complétude prouvée par grep, pas par la liste. Fallbacks nom-de-personne (3) reportés en 2bis. Non-régression CTP : visuelle uniquement (zéro logique touchée).| ✅`f0833fd` |
| 10/06 | **Hardcodes sous-lot 2bis mergé** : (3 lignes) : fallbacks 'CTP' nom-de-personne → '—'. Grep 'CTP' quoté vide. Code mort détecté (nomFranchisee chantiers, non rendu) → bloc D. Hypothèse « Bonjour ${prenom} » infirmée par audit.| ✅ `59c0112` |
| 10/06 | **Hardcodes sous-lot 3 mergé**:  (2 lignes) : titre portail client dynamique via embed agence. RLS client→agences prouvée en base (policy + helper + données). Premier refus de validation du chantier : audit initial affirmait sans montrer — preuves brutes exigées et obtenues. Réserve : garde-fou societe_id à la création de profil client → bloc D.| ✅ `96c7362`|
| 10/06 | **SÉCURITÉ** : faille cross-tenant /api/cr (découverte par l'audit du sous-lot 4 hardcodes). Fix mergé (fix/cr-route-ownership, +29) : appartenance RLS-miroir + docsPaths double contrôle. Prouvé par 8 tests E2E console (Bearer token via localStorage, body complet typeVisite/notesBrutes/imagesBase64/docsPaths). Conséquence : audit transversal routes service_role ajouté bloc A (bloquant). | ✅ `fd2b2cd`|
  LEÇON PROCESS : 8 tours de console perdus faute de connaître le contrat du body. Désormais tout prompt de fix sur une route API DOIT exiger en livrable : (1) la signature exacte du body attendu, (2) un exemple de requête valide rejouable, (3) la commande de test négatif cross-tenant. Ne plus brute-forcer les noms de champs depuis la console.
| 10/06 | Hardcodes sous-lot 4 mergé (+4/−4) : nom d'agence dynamique dans le prompt système CR. Chantier hardcodes SOLDÉ (5 sous-lots). Réserve sep_*.js → bloc D. | ✅ `2a165b6` |
| 10/06 | SÉCURITÉ : create-agente PATCH/DELETE corrigé. 2e route du pattern service_role (la plus grave : destruction cross-tenant via deleteUser). Contrôle société avant toute mutation, ordre critique respecté en DELETE. Prouvé E2E (404/404/200 + cible TEST AI intacte en base). | ✅ `ca64421` |
| 10/06 | SÉCURITÉ : pdf branche staff corrigée. 3e route service_role. Contrôle d'appartenance staff + rattachement crId→dossierId. Prouvé E2E (404/404/200). | ✅ `c844e60` |
| 10/06 | DETTE/RGPD : purge Storage entités (agente + artisan), branche fix/storage-purge-entites. Commit 1 agente (kbis/rib/factures) — bug d'ordre découvert en test (purge avant deleteUser → fichiers détruits si échec), corrigé : purge APRÈS deleteUser réussi. Commit 2 artisan (balayage fiches/ scopé). BONUS : bug google_tokens bloque deleteUser des agentes réelles → bloc A. Prouvé E2E ×3. | ✅ `219720e` |
| 10/06 | Session sécurité majeure. Audit transversal routes service_role → 4 failles du même pattern. Corrigées+prouvées E2E : cr (fd2b2cd), create-agente PATCH/DELETE (ca64421), pdf (c844e60). Calendrier en quarantaine. Purge Storage soldée (219720e) — bug d'ordre purge/deleteUser rattrapé en test. Bug google_tokens (suppression agente impossible) découvert. DÉCISION PRODUIT : modèle pricing per-agence/per-agente + gating par quota acté (structure figée, montants en étude). | ✅ session |
| 10/06 | SÉCURITÉ : surface anon auditée (fonctions + tables). Clean sauf policy INSERT notifications (TO public WITH CHECK true → forge anon possible). Re-scopée TO authenticated. Prouvé (INSERT anon → 42501). Dernier point sécurité bloc A. | ✅ SQL appliqué |
| 10/06 | DETTE : code mort nomFranchisee (chantiers) supprimé + redevance_debut ajouté à la création d'agente (était ignoré au POST, persisté seulement au PATCH). Branche fix/dette-nomfranchisee-redevancedebut, 2 commits. | ✅ cf13a14 |
| 10/06 |Garde-fou création profil client** | ✅  |
| 10/06 | LOT 5a — Édition d'agence. | ✅ `32915f1` mergé `ed41f57` |
| 10/06 | LOT 5b — Objectif CA par agence (UI multi-agence). UI ≥2 agences (éditeur par agence) + chargement par-agence indexé. BUG découvert au test : sauvegarderObjectif n'écrivait rien en multi (select sans agence_id → maybeSingle multi-lignes → INSERT en conflit 23505, erreur avalée → faux ✓). FIX dfac076 : select scopé par agence_id + .error lu et throw partout (upsert écarté : index partiel non ciblable par PostgREST, 42P10 prouvé). Prouvé en base après fix (montants modifiés persistés). Leçon : « marche en mono » ≠ « marche en multi ». | ✅ `ea58771`+`dfac076`, mergé 3776655 |
| 10/06 | POINT 5 — Finances affichage. (1) Libellé colonne/RepartRow : nom de l'admin (« Marine ») → « Société » (cohérent Lot 2) + suppression code mort nomFranchisee/adminRes (requête profiles admin inutile retirée). (2) KPI « Net à virer à l'agente » supprimé (concept F1−F2 trompeur, deux flux séparés). ⚠️ Audit s'était trompé (net censé alimenter que le KPI → faux, alimente aussi la colonne « Net » du tableau) — colonne CONSERVÉE (nom à trancher). | ✅ `5081784` + (commit 2), mergé  |
| 11/06 | BUG F2 (1/3) — doublon factures_agente. upsertFactureMoisType (select-puis-insert sur état mémoire, sans garde DB) avait créé 2 lignes identiques (Anne-Lise, 2026-06, ctp_vers_agente, double-insert à 2s). factures_agente n'avait AUCUN index unique. Fix données : DELETE ligne en double + CREATE UNIQUE INDEX (agente_id, annee, mois, type_facture). SQL versionné docs/sql/fix_doublon_factures_agente.sql. Reste : durcir upsertFactureMoisType (gérer 23505) + régler redevance_mensuelle_ht. | ✅ SQL appliqué + e8a44d3 |
| 11/06 | BUG F2 (2/3) — réglage montant redevance. profiles.redevance_mensuelle_ht existait mais saisissable nulle part → redevance live=0 pour agentes à NULL (juillet ne se déduisait pas). Ajout champ « Redevance mensuelle (HT) » au formulaire agente (création + édition, modale partagée) + route create-agente (POST+PATCH, validation nombre≥0), calqué sur redevance_debut. Bonus : date redevance désormais saisissable dès la création (avant : édition seule). Reste : re-saisir montants Marie/Manon/TEST2 + durcir upsertFactureMoisType. | ✅ 97ea172 |
| 11/06 | BUG F2 (3/3) — durcir upsertFactureMoisType. Double-clic sur toggle F2 (onClick sans garde) + existence testée sur état mémoire périmé → double-insert (cause du doublon). Fix 2 commits : (1) garde de ré-entrance (useRef synchrone, return si déjà en vol, finally remet false) ; (2) écriture robuste — existence EN BASE sur 4 clés, .error lu partout + throw, 23505 rattrapé (relit+update, pas de doublon), UPDATE ciblé (pas d'upsert → pas de clobber montant/statut au dépôt PDF), feedback erreur sur les 3 appelants. Trois couches : garde + index unique + rattrapage 23505. | ✅ `280dbe7` + (commit 2), mergé f183014 |
| 11/06 | DETTE UX — boutons morts Finances supprimés. 4 boutons décoratifs sans onClick retirés (CSV, ▼, « Exporter le bilan », « Saisir un règlement ») + 2 divs conteneurs devenus vides. Grep : aucun handler à nettoyer (jamais câblés). Décision : export NON construit (besoin non avéré, à faire si demandé avec le demandeur). | ✅ `43db86d`, mergé b64b30e |
| 12/06 | DETTE E-bis — suppression formulaire devis inline MORT (chantiers/[id]). Cluster autoréférent prouvé à l'audit : 7 symboles (ajouterDevis, savingDevis, devisEnEdition, nouveauDevis, setND, sauvegarderDevis, modifierDevis), zéro lien vivant, zéro JSX. −56 lignes. Chemin vivant DevisModal+saveDevisFromModal intact. Logique contrat_signe préservée (dupliquée dans le vivant). Faux ami `set`(dossier) conservé. | ✅ `8512687`, mergé 63b0cd9  |
| 12/06 | LOT A cosmétique espace-client + paramètres (branche fix/cosmetique-espace-client-params, 4 commits). (1) `774fd28` intégrations dev masquées (Supabase, IA Claude). (4) `5da3f3a` Google Drive retiré (bouton mort ; piste future = backlog). (2) `2836370` bulles messages client : styles permutés corrigés (client = bleu/droite, agence = gris/gauche ; logique auteur_role déjà saine). (3) `c850d02` statuts internes masqués côté client : badge override « Dossier en préparation » pour a_contacter/a_relancer + stepper fondu 5→4 étapes (« Préparation » remplace À contacter/À relancer). lib/dossiers.js (calcStatut/STATUT_CONFIG staff) NON touché → écrans agente/admin voient les vrais statuts. | ✅ mergé c40f983 |
| 12/06 | LOT CR markdown→rendu (branche fix/cr-markdown-rendu, 4 commits + SQL). Cause racine : join('') assemblait les sections en collant les titres ## en milieu de ligne + 2 espaces d'indentation parasites du template fuyaient dans la donnée. (1) `7028d77` fix source : join('\n\n') + template désindenté. (2) `26b2f76` composant partagé MarkdownCR (variant client/staff, parsing extrait à l'identique, filet legacy regex pour titres collés) + branchement client + fallback notes_brutes → « CR en cours de rédaction » (confidentialité). (3) `6f33025` côté agente : aperçu strippé + dépliable complet variant staff. (4) `44d3e75` migration SQL des 5 CR legacy (désencollage + désindentation, backup rollback intégral, scopé WHERE). Audit : 5/5 collés, 0 faux positif. PDF non impacté (re-parse indépendant). | ✅ mergé  f385f27 + SQL appliqué |
| 12/06 | DETTE E-bis — erreurs avalées chantiers/[id], écritures 💰 (Lot 1, branche fix/erreurs-avalees-chantier-finances, 3 commits). Audit ciblé : 18 handlers / ~33 écritures sans lecture de .error → faux ✓ possibles sur l'argent. Aucun optimisme pré-écriture (zéro rollback). Pattern : const {error}=await…; if(error){setErreur;return} avant refetch/setState/setSucces ; capture des maybeSingle() avalés (anti-doublon suivi_financier). C1 `e92c441` suivi_financier (G/I/J + K filet temporaire + F message partiel reste-en-édition + L throw active le catch). C2 `1b63379` factures (B faux✓ éliminé+saisie préservée, C/D/E). C3 devis (A/M/N/O/P/Q faux✓/R/S inline). | ✅ mergé 5efbe04 |
| 12/06 | DETTE E-bis — erreurs avalées chantiers/[id] (Lot 2, branche fix/erreurs-avalees-chantier-divers, 3 commits). `da1cc94` écritures non-💰 (photos, RDV, interventions, fiches, docs, messages lu_agence, CR publier/supprimer/valider) au pattern Lot 1. `b866d73` pdf_path : feature morte supprimée (comptes_rendus.pdf_path n'existe pas → update échouait à 100% en silence + uploads orphelins ; ligne+upload+champ fichier retirés ; 0 lecteur confirmé ; CR reste texte + PDF à la volée api/pdf). Commit dossiers : 3 taux inline (taux_courtage/honoraires_amo_taux/apporteur_actif) avec ROLLBACK sur erreur (pessimiste écarté : inputs contrôlés onChange → intypables) + 2 contrat_url. → TOUTES les écritures de chantiers/[id] capturent désormais leur erreur. | ✅ mergé 49953e4 |
| 12/06 | DETTE E-bis — erreurs avalées hors chantiers/[id] (Lot 3, branche fix/erreurs-avalees-espace-planning-artisans, 3 commits). `6fdee53` espace-client message client (CLIENT-FACING : feedback non technique « réessayez », texte conservé, msgErreur local calqué sur pdfErreur). `608a7e3` planning delete RDV/intervention (capture delete Supabase, Google deleteGoogleEvent quarantaine non touché). + artisans (uploadFichier optimiste post-write avec spinner libéré, supprimerFiche, supprimerSelection en boucle → accumulateur erreurs[] existant). artisans/nouveau sauté (déjà arbitré). → sujet « capturer les erreurs » couvert sur toute l'app hors quarantaine calendrier. | ✅ mergé (+ hash) |
| 12/06 | LOT K — atomicité du toggle honoraires (branche fix/k-atomicite-suivi-honoraires, mergé 62086ec). Cause : majSuiviChantier écrivait le couple honoraires_courtage+acompte_amo (même encaissement, vue courtage/AMO) en 2 upserts non atomiques → échec partiel = désync (filet temporaire posé au Lot 1). Fix : fonction Postgres suivi_toggle_honoraires (SECURITY INVOKER → atomicité plpgsql + RLS héritée, zéro re-check d'appartenance), upsert via index partiel existant (dossier_id, type_echeance) WHERE artisan_id IS NULL, DO UPDATE préserve montant_ttc. Client résout p_types (1-2 lignes, miroir AMO), 1 rpc() remplace 2 upsertOne + filet. SQL versionné docs/sql/k_suivi_toggle_honoraires.sql. Tests OK : T1-T2 coche/décoche couple (2 lignes ensemble), T3 solde seul (1 ligne), T4 préservation montant_ttc (1234 inchangé), T5 atomicité (échec 2e ligne → 1re rollback, count=0), T6 cloisonnement (agente autre société → accès refusé). Décision SECURITY INVOKER (pas DEFINER) : le contexte n'exige aucun bypass RLS, INVOKER donne atomicité + cloisonnement gratuit. | ✅ SQL appliqué + mergé 62086ec |
| 12/06 | FIX #1 — Bug statut chantier←devis (branche fix/statut-chantier-devis, 52b0d90). calcStatut (lib/dossiers.js) déclenchait devis_a_modifier sur un devis 'refuse' (logique périmée, écrite avant que 'a_modifier' existe). Fix : devis_a_modifier ⟸ hasAModifier (devis.some statut==='a_modifier', enfin branché) OU tousRefuses (devis.length>0 && every 'refuse'). hasR3 retiré (R3 faite → retombe devis_en_attente via hasR2 ; prouvé en base : R3-sans-R2 = 0 cas). Un devis refusé isolé ne fausse plus le statut. Impact liste chantiers + espace client (badge rouge faux → jaune juste) ; colonne persistée + changerStatutDevis intacts. | ✅ mergé ddfaf72|
| 15/06 | LOT #3 — Atomicité des conversions AMO↔courtage (branche fix/atomicite-conversions-amo). Cause : convertirEnAMO/convertirEnCourtage faisaient une séquence multi-écritures (update dossiers.typologie + rename type_echeance + create/delete solde_amo) NON atomique (try/catch+throw seulement) → échec partiel = état incohérent (typologie changée, lignes non migrées). Fix : 2 fonctions Postgres convertir_dossier_en_amo(p_dossier_id, p_taux_amo) et convertir_dossier_en_courtage(p_dossier_id), plpgsql SECURITY INVOKER (atomicité + RLS héritée, patron K). Rename ne touche que type_echeance → montant/date/statut préservés (encaissement acté conservé). IF FOUND → solde créé seulement si ligne courtage existait ; ON CONFLICT DO NOTHING idempotent. Collision d'unicité = échec atomique (rollback total, mieux qu'avant). Client : 1 rpc() par sens remplace la séquence. SQL versionné docs/sql/conversions_amo_courtage.sql. Tests OK : T1/T3 préservation (7664.26/regle/date conservés), T2 AMO→courtage, T4 atomicité (collision → typologie inchangée), T6 sans-ligne, T5 cloisonnement. | ✅ SQL appliqué + mergé 2d56011|
| 15/06 | LOT #4 — Rattrapage data couples AMO legacy (branche fix/rattrapage-couples-amo-legacy). 2 dossiers antérieurs au code actuel avec incohérences : AM-002 (acompte_amo.date_paiement NULL ← courtage 2025-12-23 ; honoraires_courtage.montant_ttc NULL ← acompte 3982.09), AM-009 (acompte_amo.date_paiement NULL ← courtage 2026-05-13). Règle : la part courtage fait foi pour la date, l'acompte pour le montant (même encaissement). SQL versionné docs/sql/rattrapage_couples_amo_legacy.sql : backup table datée + SELECT avant/après + 2 UPDATE scopés (filtres IS NULL → seuls AM-002/AM-009 matchent, confirmé en base) + COMMIT manuel + rollback commenté. Counts : DATE 2 lignes, MONTANT 1 ligne. Rattrapage préventif (rien ne lit acompte_amo.date_paiement). Pur data, 0 code/schéma. | ✅ appliqué + mergé 15767f7 |
| 15/06 | LOT #5 — Versionner les policies Storage live (branche chore/versionner-policies-storage). Les policies storage.objects avaient été recréées via dashboard → noms divergents, repo obsolète. Créé docs/sql/storage_policies.sql = export FIDÈLE des 8 policies live (2 buckets privés documents+photos, recopie verbatim de pg_policies, jointures de scoping tenant intactes, exemption placeholder conservée pour #7). Nouvelle source de vérité. Supprimé 5 fichiers SQL Storage obsolètes. Pur versionnement, 0 modif base. | ✅ mergé f06bb09 |
| 15/06 | #6 — Branding PDF sep_*.js : CLOS SANS CODE. Audit : les 9 lib/sep_*.js sont des images base64 (pages de séparation PDF), aucun texte paramétrable, zéro branding en dur. L'exclusion lib/sep_ aux sous-lots hardcodes était inoffensive. | ✅ clos (constat) |
| 15/06 | LOT #7 — Fermer la brèche placeholder cross-société (branche fix/placeholder-cross-societe). 6 policies Storage avaient une exemption (name ~~ '%.emptyFolderPlaceholder') court-circuitant le scoping tenant → capacité cross-tenant (lister/créer/supprimer un placeholder chez un autre tenant ; inoffensif, 0 octet, mais inutile). Fix : DROP+CREATE des 6 policies sans l'exemption (jointures de scoping intactes, texte repris depuis pg_policies live) — vérifié 8 policies a_exemption=false en base, T1 non-régression OK. DELETE du placeholder via UI Storage (SQL direct bloqué par storage.protect_delete de Supabase ; non bloquant car l'exemption retirée soumet le placeholder au scoping normal). + RÉPARÉ storage_policies.sql : ajout des 2 policies DELETE manquantes (omission de l'export #5) → source de vérité complète à 8 policies sans exemption. | ✅ appliqué + mergé 0ee864c |
| 15/06 | SESSION D — espace client (branche fix/espace-client-cr-realtime-avancement, 2 commits). #9 `92043cf` : retrait de la barre « avancement » morte (dossiers.avancement = 0 sur 31 dossiers, jamais alimentée, gate >0 → jamais affichée ; vestige maquette _design). Barre + passe-plat chantiers/[id]:1142 + select explicite retirés. Stepper 4 étapes (calcEtape) = seule progression client, intact. calculerAvancement (staff, % encaissements) non touché. Colonne laissée en base (orpheline, candidate DROP COLUMN futur). #8 (realtime CR) : chargerComptesRendus appelé 1× au montage → CR publié invisible côté client sans refresh. Fix : ALTER PUBLICATION supabase_realtime ADD TABLE comptes_rendus (docs/sql/realtime_comptes_rendus.sql) + 2e .on('postgres_changes', event:'*', table:comptes_rendus, filter dossier_id) sur le channel existant. Cloisonné (RLS subscriptions + filter). Testé 2 onglets : CR apparaît/disparaît en temps réel. | ✅ SQL appliqué + mergé 5f7e3cd |
| 15/06 | CHORE — DROP COLUMN dossiers.avancement (branche chore/drop-avancement-orpheline). Colonne orpheline depuis #9 (vestige maquette _design, 0 partout, plus aucun usage code vivant). Dépendances vérifiées avant DROP : 0 vue/fonction/index, 1 CHECK intrinsèque (part avec la colonne), 0 import _design depuis l'app. docs/sql/drop_avancement_orpheline.sql : contrôle AVANT/APRÈS, sans IF EXISTS (échec bruyant), rollback documenté (integer NOT NULL DEFAULT 0 + CHECK 0-100). DDL irréversible, données valaient 0 (rien perdu). | ✅ appliqué + mergé (+ hash) |
| 15/06 | LOT #10 — Types de RDV (branche feat/types-rdv, 2 commits). Audit : rendez_vous.type_rdv (CHECK fermé 4 valeurs) ≠ comptes_rendus.type_visite (5 valeurs), champs indépendants. 72% des RDV en 'autres' (fourre-tout) → évolution de feature, pas bug. Commit 1 (a2a7d92) : SQL extend_type_rdv_check.sql, CHECK étendu 4→7 valeurs (+ suivi/reception/etude), appliqué BEGIN/COMMIT contrôle avant/après. Commit 2 (a617134) : TYPE_CONFIG + select + tableau types dans planning ET chantiers/[id] (2 formulaires RDV) ; règles conditionnelles — suivi/reception/etude exigent un dossier (branche ≠'autres'), reception active l'artisan comme R2, suivi/etude sans artisan ; 'autres'/R1/R2/R3 préservés. + 2-bis : nettoyage artisan_id au changement vers un type sans artisan (unidirectionnel, dans le onChange/onClick, pas au montage). Commit 3 (fix reception CR manuel) ANNULÉ : reception déjà présent (audit périmé). Valeurs alignées sémantiquement sur les CR. | ✅ SQL appliqué + mergé e5c6da0 |
| 15/06 | SESSION QUICK WINS — 4 fixes chantiers/[id] (branche chore/quick-wins-chantier, 4 commits, mergé 92c2ce5). QW1 (e709568) : suiviAcompteAMO vestige mort supprimé (0 usage). QW2 (2d344ac) : prenomAdmin fetch .order('prenom').limit(1).maybeSingle() → ne lève plus si ≥2 admins (RLS cloisonne, pas de filtre societe_id ajouté). QW3 (fe93443) : libellé suppression chantier dédupliqué → constante ENTITES_CHANTIER injectée au confirm() ET au sous-titre bouton (divergence CR/comptes-rendus + suivis/suivis financiers fermée). QW4 (...) : taux_courtage + honoraires_amo_taux passés de UPDATE-par-frappe à onBlur+Enter. onChange garde le set() state (UI + recalcul live instantanés) ; supabase.update déplacé en onBlur via persistTaux() factorisé ; Enter→blur() ; rollback erreur préservé (valeur pré-édition capturée au onFocus dans un ref). apporteur_actif (toggle) intact. Limite assumée : quitter sans blur (back navigateur) ne persiste pas. | ✅ mergé 92c2ce5 |
| 15/06 | RÉGULARISATION RDV LEGACY (chantier statut, prérequis) — 11 RDV réels de Marine insérés (dates fournies par Marine) : 10 R3 presentation_devis passés (ZIAT×2, LEULIER, DREBEL, PETER, CHAUVIN, DURAN, COSSANTELI, KARCHAOUI CT-022, LABOURAYRE) + 1 R2 visite_technique_artisan (ODDOS). docs/sql/regularisation_rdv_legacy_marine.sql. ⚠️ LEÇON : SQL editor Supabase ne maintient pas BEGIN entre 2 exécutions séparées → INSERT direct auto-commit (la 1ère tentative BEGIN/INSERT/RETURNING a été auto-rollback, RETURNING trompeur). THOBY (AM-027) → dates chantier ajoutées. CT-014 mis de côté (devis accepté inconnu). → 10 dossiers calculent en_attente_signature, ODDOS devis_en_attente. | ✅ mergé 2841c78 |
| 15/06 | CHANTIER STATUT — DESIGN cascade calcStatut v2 (à froid, terrain en main) — Refonte taxonomie : 11 statuts, source vérité unique calcStatut, dossier.statut réduit aux overrides manuels (NULL/annule/termine). 4 nouveaux : en_etude, devis_prets, en_attente_signature, chantier_a_venir. Signaux corrigés : contrat_signe = mandat R1 (PAS signature devis, retiré des gardes) ; jalons RDV pas CR (R3=presentation_devis, R2=visite_technique_artisan, lus passés) ; devis accepte = signé, recu = présenté pas tranché ; chantier_a_venir = ≥1 accepte ET 0 recu. Ordre clé : devis_a_modifier AVANT chantier_a_venir. Verrouillé dans docs/SPEC_redesign_statut.md. | ✅ mergé a19de34 (+ maj 5ac386f) |
| 15/06 | CHANTIER STATUT — ÉTAPE A (code calcStatut) — branche feat/calcstatut-v2, 3 commits. C1 (07021af) : calcStatut réécrit cascade v2 (lit rendez_vous, tolérant []/undefined, dates en DATE seule via dateOnly, ne lit plus comptes_rendus). C2 (c87428a) : création dossier chantiers/nouveau écrit statut:null. C3 : suppression auto-push devis_a_modifier dans changerStatutDevis (chantiers/[id], calcStatut le dérive). | ✅ mergé 3043fac |
| 15/06 | CHANTIER STATUT — ÉTAPE B (NULL des calculables) — docs/sql/statut_rattrapage_null.sql. Backup backup_statut_rattrapage_20260615 (31 lignes, à DROP après étape D). UPDATE générique SET statut=NULL WHERE statut NOT IN ('annule','termine'). Appliqué : UPDATE 28, APRÈS = NULL 28 / annule 2 / termine 1, calculables_restants 0. dossier.statut ne porte plus que les overrides manuels. | ✅ mergé c596d55 |
| 15/06 | CHANTIER STATUT — F-config (STATUT_CONFIG) — 4 nouveaux statuts ajoutés à STATUT_CONFIG (lib/dossiers.js), 11 au total, ordre funnel. Couleurs Tailwind : en_etude slate, devis_prets teal, en_attente_signature amber, chantier_a_venir indigo. ⚠️ Découverte : 6 mappings statut→affichage dupliqués (dashboard STATUT_STYLE+buckets, clients, espace-client, finances, shared.jsx tones) → traités en étape C (stratégie : centraliser sur STATUT_CONFIG). | ✅ mergé 611fb22 |
| 15/06 | CHANTIER STATUT — C1 socle (StatutBadge) — StatutBadge (shared.jsx) basculé de STATUT_TONES (6 tones partagés, badge-* CSS) vers STATUT_CONFIG.color (11 couleurs Tailwind distinctes). Stratégie a : span .badge + cfg.color, sans toucher le Badge générique (intact, utilisé ailleurs). STATUT_TONES supprimé (mort). Format préservé (pill/dot), seule la couleur change. JIT Tailwind OK (classes déjà générées via espace-client). | ✅ mergé 036aeca |
| 15/06 | CHANTIER STATUT — C2→C6 réconciliation 5 écrans (branche feat/liste-chantiers-rdv) — C2 (4afb444) liste : +rendez_vous au select. C3 (626df50) fiche : badge sur calcStatut({...dossier, devis_artisans:devis, rendez_vous:rdvsDossier}) — states frais (anti-péremption). C4 (09dbdc1) clients/[id] : StatutBadge local + map STATUTS supprimés → StatutBadge partagé ; clientActif (logique) sur calcStatut. C5 (2fea54c) dashboard : +rendez_vous+date_demarrage+contrat_signe au select, calcStatut partout (enCours/aRelancer/KPI), STATUT_STYLE mort supprimé, Pipeline → 6 buckets (À traiter/En étude/En attente signature/Chantier à venir/En chantier/Terminé, annule exclu), activité récente sur calcStatut. C6 (565390c) finances : badge ternaire sur calcStatut (corrige annule/termine affichés "En cours" depuis étape B), +rendez_vous. Aucun calcul financier altéré (reposent sur suivi_financier). | ✅ mergé edc21cf |
| 16/06 | CHANTIER STATUT — ÉTAPE D (CHECK strict) — docs/sql/statut_check_strict.sql. DROP CHECK legacy dossiers_statut_check (7 valeurs) → défaut colonne NULL → ADD dossiers_statut_manuel_check CHECK (NULL/annule/termine). Exécution tout-ou-rien (garde-fou DO pré-vérif + test violation a_contacter rejeté + COMMIT auto-protégé). Scelle la réconciliation : la colonne ne peut plus re-porter une valeur calculable. | ✅ mergé 03c3cde |
| 16/06 | CHANTIER STATUT — ÉTAPE E (éditeur manuel) — Boutons contextuels header fiche : statut NULL → "Marquer terminé"/"Annuler le dossier" ; statut forcé → "Ré-ouvrir le dossier" (=NULL=recalcul). Handler majStatutManuel optimiste+rollback, confirm sur Annuler seulement. Seul chemin vers 'termine'. CLÔT le statut staff (A→E). | ✅ mergé 15f9564 |
| 16/06 | MÉNAGE CODE MORT finance.js — branche chore/finance-code-mort, 2 commits. getPartAdmin supprimée (4ce74bb, mort confirmé) + 3 clés de return mortes (calculateDevisFinance.statut/.montantTTC, calculateHonorairesRecuAccepte.totalDevisHTRecuAccepte). Variables internes gardées (montantTTC alimente acompte/solde). Aucun calcul touché. | ✅ mergé b4d7ae2 |
| 16/06 | AUDIT CODE MORT finance.js REPO-WIDE — Cartographie exhaustive. finance.js globalement propre, apporteur.lines VIVANT (faux suspect). Mort réel modeste (1 fonction + 3 clés). .ht honoraires + export superflus laissés. | ✅ (audit) |
| 16/06 | NOM CLIENT (« null » + couples) — branche fix/nom-client-format, 3 commits. formatNomClient centralisé lib/clients.js (261b91e, Option A mutualisé). 9 sites couple-aware basculés (8503e51, UI+PDF). finances + PDF récap en couple (8ca9cf4). Dashboard compact, clients/page intact, calendrier exclu. Cause : prenom2 rempli + nom2 vide (4 dossiers). | ✅ mergé 843eac6 |
| 16/06 | INDEX FK NON INDEXÉES — branche perf/index-fk. 29 CREATE INDEX IF NOT EXISTS (docs/sql/index_foreign_keys.sql), 14 CASCADE. Idempotent, non destructif. Noms colonnes vérifiés (clients.referente ≠ referente_id). Vérif post = 0 FK non indexée. Advisor nettoyé. | ✅ mergé fa86b34 |
| 16/06 | CR DANS DOCUMENTS (l.261, A+B) — Lot 1 colonne categorie text (537abec, CHECK souple). Lot 2 auto-cat à l'upload (helper detecterCategorieCR anti-faux-positifs) + toggle manuel + rétro-cat 4 CR existants (docs/sql/retro_categorie_cr.sql). Lot 3 section "Documents marqués CR" sous les CR générés (onglet CR). | ✅ mergé 86f529c |
| 16/06 | MODIFIER DOSSIER → MODALE (l.262) — Fix bug UX : "Modifier" swappait l'onglet Aperçu → invisible ailleurs. Passé en ModalShell, 5 cartes déplacées (form, apporteur, contrat, conversion, zone dangereuse), accessible depuis tout onglet. État mode retiré, handleSave inchangé. | ✅ mergé 31a20c5 |
| 16/06 | DOUBLE-CLIC LISTE CHANTIERS — feat/dblclick-ouvrir-dossier. onDoubleClick sur ligne → router.push fiche (= bouton Ouvrir). userSelect:none (anti-sélection texte). Simple clic (onSelect/preview) préservé. | ✅ mergé f607ef7 |
| 16/06 | CONTRAT AUTO-SIGNÉ — feat/contrat-auto-signe. uploadContrat : un seul UPDATE atomique { contrat_url, contrat_signe:true, date_signature_contrat:today } si pas déjà signé. supprimerContrat : UPDATE { contrat_url:null, contrat_signe:false, date_signature_contrat:null } symétrique. Case manuelle préservée. handleSave/stepper/calcStatut non touchés. | ✅ mergé 044d464 |
| 16-17/06 | PERF RLS INITPLAN — perf/rls-initplan. 3 policies notifications (auth.uid() brut → (select auth.uid())). Reste = déjà encapsulé depuis MT/L5 (le « 32 » advisor était obsolète). 0 ligne vérif post-apply. | ✅ mergé e5cd604 |
| 17/06 | RESTITUTION — factures + RIB/KBIS franchisé — feat/restitution-factures-rib-kbis. Factures : bloc legacy devis_artisans.facture_path remplacé par boucle sur factures_artisans.pdf_path (acompte/intermédiaire/finale, filtre pdf_path non null). RIB + KBIS franchisé : fetch profil admin (maybeSingle) + fusion après bloc KBIS artisans, sous même garde !isPreSignature. Erreurs non bloquantes partout. | ✅ mergé 6ef0cfd |
| 17/06 | ZIP PHOTOS — feat/zip-photos. Bouton « Télécharger les photos » sur fiche chantier staff (conditionné photos.length > 0). Handler telechargerZipPhotos : signed URLs régénérées au clic (60s), JSZip côté client, dossiers avant/pendant/apres/maquette, nom Photos_{reference}.zip. State zippingPhotos. Pas de route API. jszip ^3.10.1 installé. | ✅ mergé aa3d8b6 |
| 17/06 | FACTURE HONORAIRE RESTITUTION — feat/facture-honoraire-restitution. Nouvelle catégorie chantier_documents 'facture_honoraire' (CHECK étendu). detecterCategorieCR → detecterCategorie (facture+honorai → facture_honoraire). Bloc Factures restitution : boucle facturesHonoraires après factures artisans (isPreSignature). autresDocs déplacés en fin de document après RIB/KBIS franchisé. | ✅ mergé 9d8bdbc |
| 17/06 | BOUTON FACTURE HONORAIRE — feat/bouton-facture-honoraire. Bouton dédié "Ajouter une facture honoraires" (btn-ghost, PDF only) force categorie='facture_honoraire' + dans_restitution=true. Badge FACT orange. Case Restitution remplacée par coche fixe verte. Toggle CR masqué. uploadDocumentChantier étendu avec options={categorie, dans_restitution}. | ✅ mergé 75f70a8 |
| 17/06 | KBIS FRANCHISÉE PARAMÈTRES — feat/kbis-franchisee-parametres. Bloc KBIS franchisée ajouté dans onglet "RIB & Kbis" (paramètres admin). Handler uploadKbisFranchise (UPDATE direct profiles.kbis_url, path kbis/{id}.{ext}, accept pdf+image). voirKbisFranchise (signedUrl). La restitution récupère kbis_url automatiquement via adminFranchise. | ✅ mergé f1e8545 |
| 17/06 | PV DE RÉCEPTION — feat/pv-reception. Colonne pv_path (text, nullable) sur devis_artisans. Handlers uploadPV/supprimerPV (path chantiers/{id}/pv/{devisId}.{ext}, sans logique statut). Bloc JSX 📋 PV de réception après devis signé. Restitution restructurée : boucle groupée par devis (devisR3) → devis → factures (f.devis_id===d.id) → PV. facturesHonoraires conservées. | ✅ mergé a763a14 |
| 17/06 | FICHE TECHNIQUE DEPUIS CHANTIER — feat/creer-fiche-tech-depuis-chantier. Mini-formulaire inline dans FichesTechPanel (nom requis, description + PDF optionnels). Auto-liaison au dossier (chantier_fiches_techniques) après création. onCreated={chargerFichesTechChantier} rafraîchit le compteur parent. Lien sortant "En ajouter →" supprimé. | ✅ mergé 235ae45 |
| 17/06 | KPI MONTANT PRÉVU/RÉEL — feat/kpi-montant-prevu-reel. Suppression KPI "Frais consultation TTC" (redondant). Remplacement "Montant chantier" par deux KPIs : Montant prévu (totalDevisTTCRecus) + Montant réel (totalDevisTTCSignes). Ordre : prévu → réel → acomptes → honoraires. Nettoyage orphelins fraisStatutConfig + const f. | ✅ mergé 3401841 |
| 17/06 | STEPPER 5 ÉTAPES — fix/stepper-5-etapes. Bug Deudon corrigé. Logique monotone : step1=contrat_signe / step2=devis.length>0 / step3=0 devis recu (tous tranchés) / step4=date_demarrage_chantier||en_cours_chantier / step5=termine. done[] dérivé en cascade OR → monotonicité garantie. Contact piloté par contrat_signe (pas toujours coché). | ✅ mergé 4ecce6f |
| 17/06 | COMPARATEUR DEVIS — feat/comparateur-devis-sql. 2 tables (comparateur_simulations + comparateur_lignes) + RLS + 3 index FK. Onglet dédié après Devis&Artisans. Colonnes Base | Simul N. Toggle inclus, montant_ttc_override éditable (bleu si modifié), taux courtage+AMO par simulation. Calculs : totalTTC, honCourtage, honAMO, totalCourtage, totalAMO. Synchro auto lignes manquantes (skipSync anti-boucle). Sauvegarde auto, lazy loading. | ✅ mergé 8faf611 |
| 17/06 | FINANCES LOT 1 — refonte/finances-lot1-renommage. Renommage onglets : F1 Prévisionnel → Prévisionnel / F2 Réel → Réel. Keys et badges F1/F2 inchangés. | ✅ mergé 1f153ae |
| 17/06 | FINANCES LOT 2 — refonte/finances-lot2-menage. D4 KPI CA réel net admin only (retiré pour agente). D6 useMemo rowsReelScoped+rowsAnneeScoped. D7 variable morte chantiersAnneeEnCours supprimée. D8 sur-fetch apporteur_affaires/nom retiré. D9 anneeMin dynamique. D10 DEFAULT_PART_AGENTE centralisée finance.js. D11 typo \n → br. | ✅ mergé 0439a85 |
| 17/06 | FINANCES LOT 3a — refonte/finances-lot3a-style. D13 extraction SyntheseView/FacturationAgentes/SuiviCTPChart hors composant Finances (props explicites, stable entre rendus). D5 style unifié : Tailwind → tokens CSS (--ink/--brand) dans renderTableauPeriode, renderSuiviFinancier, renderAnnuel, SuiviCTPChart. Zéro résidu Tailwind. | ✅ mergé c064f94 |
| 17/06 | FINANCES LOT 3b — refonte/finances-lot3b-suivi-fusion. Suppression onglet Synthèse (fusionné dans Suivi). Onglet défaut → suivi. Toggle Agence/Société pour tout admin. Mode agent pour agente. Deux donuts côte à côte Réel/Prévi. Suppression "Filtré sur". Reset scope handleTab. Graphe après tableau (mois et année). | ✅ mergé 6b95794 |
| 17/06 | FINANCES LOT 3c — refonte/finances-lot3c-comptes. Correction vue Agence/Agent (redevances exclues des produits, apporteurs en reversement). Détail mois Réel uniquement. Reset scope='tous' (handleTab). Suppression (+)(−)= libellés. Suppression vue "Par mois" Prévisionnel. Vue Année Prévisionnel drill-down chantiers. Vue Année Réel drill-down mois→chantiers. agrégerPrevi mémoïsé. SuiviCTPChart restauré vue Année ASC. Charges→Reversements. libellePerimetre supprimé. | ✅ mergé 99291fc |
| 17/06 | FINANCES LOT 4 — fix/finances-royalties-reel. D3 royalties réel corrigées : royaltiesReelVal étendue à (fraisNet + comReelNet + honReel) × 0.05/0.95. Alignement prévi/réel sur les 3 catégories. | ✅ mergé 2a8ae4d |
| 18/06 | PDF CR — GLYPHES & ASSAINISSEMENT — claude/hopeful-babbage-dajunl. Police : Roboto Regular+Bold embarqués séparément (fin du « tout en gras ») + glyphes Unicode parasites corrigés. Nom fichier CR : DATE_CR_NOM (nom de famille seul). Assainisseur sanitizePdfText (app/api/pdf/route.js) appliqué au contenu CR avant rendu : flèches →/↔/← → ASCII (->,<->,<-), alertes ⚠/‼/❗ → (!), autres emoji retirés, accents/€/°/² préservés. Cause : Roboto ne couvre pas →↔⚠️ → react-pdf fallback Helvetica résiduel (déformait ’/” ou supprimait). Régénération à la volée → CR existants corrigés au prochain téléchargement. 4 commits (6abee1d/9addd10/b9e21fe/059712c). | ✅ mergé e32a9c8 |
| 18/06 | RÉPARTITION FINANCES BRUT + RÉEL ENCAISSÉ — feat/finances-repartition-brut-reel. Répartition par chantier (dépliée + colonnes + tfoot) : lignes en BRUT (Frais+Com+Hon−Royalties=Net), réel = composants ENCAISSÉS (comBruteEncaissee, honReelBrut, fraisBrutReel, royaltyReelle), prévi = ENGAGÉS base actifs (honPreviBrut, royaltyPreviActifs). Net JAMAIS touché (champs ajoutés only). Arrondi royalty par composant = méthode illiCO (royalty sur chaque encaissement, NE PAS recalculer sur total). Identité bouclée au centime prévi+réel sur 5 dossiers (AM-001/002/009/021/027), Net inchangé. | ✅ mergé 53dbf13 |
| 18/06 | OBJECTIF AGENTE SCOPÉ — feat/objectif-agente-scope. Bug : agente (agenceActive=null) comparait son réel à la SOMME des objectifs de toutes les agences (209 166 €/mois affichés). Fix affichage l.1842 ciblé !isAdmin : objectif perso (cible='agente', profile.id) → fallback objectif de SON agence (label « Objectif agence ») → sinon « non défini » (colonnes « — », jamais 0 comme cible). Admin byte-identique (getObjectif('agence') consolidé + vue agence). Net non touché. Contrôle 4 cas sur agentes réelles. | ✅ mergé db506e3 |
| 18/06 | RENOMMAGES FINANCES — feat/finances-renommages. 7 strings d'affichage (texte seul). Tab bar : badges F1/F2 retirés (Prévisionnel/Réel seuls), « Suivi financier » → « Compte de résultat » (icône gardée). Compte de résultat : « Gains » → « Produits », « Total gains » → « Total produits ». Graphe SuiviCTPChart : « Gains »/« Gains encaissés » → « Produits »/« Produits encaissés ». Titres carte Prévi/Réel : préfixe « F1 — »/« F2 — » retiré. Intouchés : clés d'onglet, fonctions, Facturation F1/F2 (invoice), « Mes gains », « Reversements », « Résultat net ». | ✅ mergé e76d6b9 |
| 18/06 | COMPTE DE RÉSULTAT « CA GÉNÉRÉ » — feat/finances-ca-genere. Formule unifiée 3 modes : CA = Hon(brut)+Com(brut)+Frais(brut) − Royalties − Apporteur remboursé(TOTAL). agent/agence = ce CA ; société = CA − Parts agentes. Royalty comptée 1× (corrige double-compte société : 2997→3755 AM-002). Apporteur = TOTAL dans les 3 modes (agent passait avant la part seule). Redevance retirée des produits (→ Facturation). KPI haut « CA réel net » → « CA généré » (=totalCAGenere, avant parts) + % objectif rebranché sur cette échelle ; « Part franchisée » = totalNetCTP (après parts) ; identité CA généré = Part franchisée + Part agentes. KPI agente « Mes gains » → « CA généré ». Affichage déplié : produits bruts + ligne Royalties (1×). 5 champs bruts ajoutés à l'agrégat mensuel+annuel. calculerReelBase NON touché (net par chantier inchangé). Contrôles : 12584,05/12584,05/3754,89 AM-002, identité au centime 3 dossiers. | ✅ mergé c2cf533 |
| 18/06 | RLS objectifs_ca DURCIE + TESTÉE — chore/objectifs-ca-rls-durcissement + chore/objectifs-ca-rls-test. Policy unique objectifs_ca_scope (FOR ALL) → 4 policies : select (lecture large, fallback agence conservé), write_admin (FOR ALL société), insert_agente + update_agente (sa ligne 'agente', agence épinglée, PAS de DELETE agente v1). Ferme brèche (b) agente écrivait objectif d'agence + (c) agence_id non épinglé. Appliqué en base + 5 tests RLS verts (refus 2/4 = 42501 prouvés RLS, … DELETE agente = 0 ligne, fallback agence lu). | ✅ appliqué + testé, SQL versionné 2013b2c |
| 18/06 | OBJECTIF AGENTE — SAISIE FRONT (Lot 2a-front) — feat/objectif-agente-front. Helper app/lib/objectifs.js (saveObjectif SELECT→UPDATE/INSERT sans ON CONFLICT, getObjectifAgente maybeSingle distingue null/0). Card « Mon objectif annuel » éditable sur profil/page.js, agente only (role='agente' && agence_id), année=getFullYear(), champ vide si pas de perso (jamais le fallback agence). Écrit la ligne exacte que Finances relit → % CA généré se met à jour. RLS déjà durcie+testée (insert/update_agente). parametres non touché. | ✅ mergé c608a4e |
| 18/06 | BARRE OBJECTIF SUR CA GÉNÉRÉ (mois + année) — feat/finances-objectif-ca-genere. La barre objectif compare désormais l'objectif au CA GÉNÉRÉ du périmètre (caGenerePourCle = reelProduits − reelApporteur), plus au reelNet. Vue mois : reel = CA généré mensuel (inchangé en agent/agence où CA généré==reelNet ; en société = reelNet+parts). Vue année : nouvelle ObjectifBar = totalCAGenere vs objectif annuel (== KPI haut, même %). Labels « Objectif CA généré …/mois » et « …/an » distincts du « Réel net » du tableau. Garde objectif≤0 → « non défini » (corrige admin sans objectif). Cohérence KPI haut == barre mois == barre année. | ✅ mergé 14346as |
| 18/06 | LIBELLÉS FINANCES VAGUE 1 (B+C+A) — feat/finances-libelles-vague1. B : colonne « Net » (solde F1−F2 indicatif, jamais relue) supprimée du tableau facturation (en-tête + cellule + total + const n/net orphelines, colSpans 6→5). C : légende « Redevance due au titre du mois d'activité » sous la grille redevances (données inchangées, toujours datée activité). A : label « Honoraires (courtage + AMO) » → « Honoraires » (d.honN inchangé). Affichage uniquement, 0 calcul touché. | ✅ mergé cafe6dd |
| 18/06 | COLONNE HONORAIRES TABLEAU RÉEL — feat/finances-colonne-honoraires. Ajout colonne « Honoraires » (brut : honReelBrut réel / honPreviBrut prévi) entre Commissions et Royalties dans renderFinanceTable, ligne + total (totals.honoraires). Lève l'asymétrie d'affichage (commissions étaient en brut, honoraires fondus dans le Net). Affichage PUR : honReelBrut/honPreviBrut n'entrent pas dans le net (inchangé au centime). colSpans 9→10. Seul renderFinanceTable touché. | ✅ mergé 386ce8b |
| 18/06 | L16 FACTURATION SCOPÉE PAR AGENCE — feat/finances-l16-facturation-scope. Sélecteur d'agente du module Facturation passé de agentes (full société) → agentesScope (filtré agence active). + useEffect dans Finances() qui reset agenteSelectionnee à null quand l'agente sort de l'agence active (miroir de l'effet scope l.772). Onglet agence → agentes de cette agence seulement ; consolidé/mono-agence → toutes. Filtrage de liste pur, aucun calcul F1/F2/redevances touché. Garde-fou CTP : agenceActive null → return tôt, agentesScope = toutes, aucune régression. | ✅ mergé d3df28b |
| 18/06 | FIX MOTS COLLÉS NOTES CR — fix/cr-join-notes. [crNotes, crVocalTexte].join('') → join('\n\n') aux 2 occurrences (entrée IA l.1642 + notes_brutes stockées l.1673). Cause des « lansuite » : concaténation sans séparateur collant la fin des notes tapées au début des notes vocales. Bug de code déterministe (pas l'IA). filter(Boolean) conservé. | ✅ mergé 1b00cbd  |
| 18/06 | PROMPT ORTHOGRAPHE + TEMPÉRATURE CR — fix/cr-join-notes (lot 3). Consigne :46 enrichie (corrige orthographe/grammaire/conjugaison de la prose rédigée, mention saisie+dictée) ; :47 précisée (ne JAMAIS corriger un nom propre/raison sociale/terme technique). Les deux se complètent (corrige ta prose / préserve les noms). temperature: 0.3 ajoutée (tâche factuelle structurée, plus de constance). Modèle/max_tokens/format JSON inchangés. | ✅ mergé bde261b |
| 18/06 | PRÉSERVER PRÉNOM CLIENT CR — fix/cr-prenom-client. Consigne :47 étendue pour inclure explicitement le nom + prénom du client (maître d'ouvrage) dans l'exception « ne jamais corriger/accentuer un nom propre ». Corrige : l'IA ré-accentuait « Jerome » → « Jérôme » (le CR lit pourtant clients.prenom verbatim ; c'était la génération, pas la source). Probabiliste (consigne IA) ; CR legacy non migrés. | ✅ mergé abda085 |
| 20/06 | C7-1 BASE — Vue devis client + scoping fonction — feat/c7-vue-devis-client. Vue client_devis_acceptes (security_invoker=false) exposant UNIQUEMENT {devis_id, dossier_id, statut, artisan_entreprise} des devis statut='accepte' du client. Scoping extrait dans fonction mes_dossiers_client() (SECURITY DEFINER, STABLE, search_path figé, réutilisable). Aucune policy sur devis_artisans/artisans (restent fail-closed) ; vue = seul accès client. Droits resserrés (authenticated = SELECT seul). Étanchéité PROUVÉE : 7 tests verts (T2 inter-client même agence = 0 fuite ; T6 colonnes sensibles commission/notes/montant = 42703 inatteignables ; T5 fail-closed). SQL + test versionnés. | ✅ mergé 0bc32ae |
| 20/06 | C7-1 FRONT — Afficher devis acceptés espace client — feat/c7-devis-front. État devis + chargerDevis() lisant la vue client_devis_acceptes (.eq dossier_id), branché dans le Promise.all. Les blocs existants "X devis signés" + "Artisans sélectionnés" (UI déjà présente, masquée car embed vide) rebranchés sur l'état devis, mapping adapté (devis_id, artisan_entreprise). Embed mort devis_artisans retiré de la requête dossiers (calcStatut inchangé via || []). T3 (client voit ses devis) validé à l'écran. | ✅ mergé 0b2630a |
| 21/06 | C7-3 PDF DEVIS SIGNÉ — feat/c7-devis-pdf + feat/c7-devis-front-carte. (1) Vue client_devis_acceptes : ajout colonne a_devis_signe (= devis_signe_path IS NOT NULL, sans exposer le chemin) ; étanchéité re-prouvée (T1-T7 verts, T7c chemin caché). (2) /api/pdf étendu type='devis' : allowlist {cr,devis}, ownership strict (devisId ∈ devis du dossier possédé + statut accepte), download bucket documents en service_role (bucket reste privé), Content-Type dérivé extension, inline. (3) Front : bloc Artisans → carte cliquable (📄 ouvre le devis) si a_devis_signe, ligne simple sinon. | ✅ mergé e1c1c09 (base) + 4b2c104 (carte) |
| 21/06 | C7-4 DURCIR CR CLIENT — feat/c7-cr-vue-client + feat/c7-cr-realtime. Fuite fermée : le client pouvait lire ses R1/R2/R3 validés + notes_brutes/contenu_ia par requête directe. Vue client_comptes_rendus (definer, colonnes limitées, valide AND type NOT IN r1/r2/r3, scoping mes_dossiers_client()) ; étanchéité prouvée (8 tests : R1/R2/R3 invisibles, brouillons exclus, colonnes sensibles 42703, inter-client). Front bascule sur la vue (testé écran client A). Policy comptes_rendus_client_read retirée → table fail-closed (vérif réelle : table=0, vue=2). Realtime CR retiré (messagerie réseau intacte). reception visible (NOT IN), realtime CR sacrifié (option a). | ✅ mergé 009d104 (vue) + 4e18d8a (front) + 5805e18 (realtime) |
| 23/06 | C7-5 DURCIR MESSAGES — feat/c7-messages-durcir. (1) anti-usurpation : messages_client_insert + WITH CHECK auteur_role='client' (défaut colonne 'agente' neutralisé). (2a) messages_client_update (scopée mes_dossiers_client(), auteur_role<>'client') → le lu client fonctionne enfin. (2b) trigger messages_lock_columns_trg BEFORE UPDATE global : seules lu/lu_agence mutables, contenu+métadonnées figés pour TOUS (client ET staff), IS DISTINCT FROM NULL-safe, INVOKER. Verrou universel prouvé (postgres P1-P4) + testé écran 2 côtés (5/5). Édition 10min = feature future (UI + assouplir trigger). | ✅ mergé dad422d |
| 24/06 | AGENDA SCOPE TENANT — feat/agenda-scope-tenant. Colmate la fuite inter-agences de l'agenda (RLS ne cloisonnait que par existence du dossier). Colonnes agence_id+societe_id (nullable) sur rendez_vous + interventions_artisans, FK RESTRICT. Backfill : liés au dossier → agence/société du dossier (44 RDV + 9 interventions) ; 214 RDV orphelins → Martigues/CTP. Trigger agenda_derive_tenant() BEFORE INSERT OR UPDATE : agence_id NULL → get_my_agence_id() (créateur), puis dérive societe_id via agences.societe_id ; dérivation simple SANS clause d'immuabilité ; NULL-safe pour service_role/sync (insert passe avec NULL/NULL). RLS réécrite : (agente AND agence_id=get_my_agence_id()) OR (admin AND societe_id=get_my_societe_id()), USING+WITH CHECK. 10 tests d'étanchéité OK : inter-sociétés (T1-T3), trigger agente/service/dérivation (T4-T6), inter-agences même société (T7a/b). Imports sync (agence NULL) restent non scopés = traités à l'étape 2. Premier lot du chantier calendrier (socle, étage 1). | ✅ mergé b3dbc2f |
| 24/06 | AGENDA CONVENTION DATE — feat/agenda-date-utc. Migration date_heure (timestamp naïf) → timestamptz UTC. DÉCOUVERTE : l'hypothèse 2-populations de l'audit était FAUSSE — TOUS les date_heure (form ET imports Google) étaient en heure locale Paris (prouvé contre les google_event_id : DST respecté hiver+1/été+2). Conversion UNIFORME AT TIME ZONE 'Europe/Paris' (pas de CASE). Filet date_heure_old conservé. 6 sous-lots : (1) SQL migration + sauvegarde rollback, tests wallclock_mismatch=0/bni_mismatch=2 faux positifs expliqués (BNI déplacés jours fériés) ; (2) front lecture Europe/Paris ; (3) fix FullCalendar via plugin @fullcalendar/luxon3 (v6 ne gère les fuseaux nommés qu'avec plugin) ; (4) front écriture planning (helpers parisLocalToInstant/instantToParisLocal dans lib/dates) ; (5) front écriture fiche chantier (oubli rattrapé) ; (6) push sync (push/route + sync/route) : instant ISO complet au lieu de slice(0,19) ré-étiqueté Paris. Interventions (dates pures + heure_debut time) non concernées. | ✅ mergé edd3543 |
| 24/06 | VUES CLIENT AGENDA — feat/agenda-client-views. Expose au client (espace-client) ses RDV + interventions via 2 VUES dédiées (pattern client_devis_acceptes), PAS de policy RLS brute. Visibilité AUTOMATIQUE par type (pas de flag visible_client, pas de toggle référent) : client_rendez_vous montre R1/R2/R3/suivi/reception (libellés neutres, SANS artisan ni notes), exclut autres/etude ; client_interventions montre toutes les interventions des dossiers du client avec le NOM D'ENTREPRISE artisan seul (pas coordonnées). Filtre dossier + expiration hérités de mes_dossiers_client(). security_invoker=false. Grants moindre privilège : REVOKE ALL anon/authenticated/public puis GRANT SELECT authenticated (le grant par défaut Supabase donnait INSERT/UPDATE/DELETE à authenticated → corrigé). 7 tests d'étanchéité OK (masquage colonnes sensibles, expiration, cloisonnement, scope tenant intact). | ✅ mergé d188dd5 |
| 24/06 | VUES CLIENT AGENDA + FRONT — feat/agenda-client-views (SQL, mergé d188dd5) + feat/agenda-client-front (front, mergé f9b571d). Expose au client ses RDV + interventions dans l'espace client. SQL : 2 vues dédiées (client_rendez_vous, client_interventions, pattern client_devis_acceptes, security_invoker=false), PAS de policy RLS brute. Visibilité AUTOMATIQUE par type (pas de flag, pas de toggle référent) : RDV montre R1/R2/R3/suivi/reception (libellés neutres « Première visite/Visite technique/Présentation des devis/Suivi de chantier/Réception de chantier », SANS artisan ni notes), exclut autres/etude ; interventions montre toutes celles des dossiers du client avec NOM D'ENTREPRISE artisan seul. Filtre dossier + expiration hérités de mes_dossiers_client(). Grants moindre privilège : REVOKE ALL anon/authenticated/public puis GRANT SELECT authenticated (corrige le grant par défaut Supabase qui donnait INSERT/UPDATE/DELETE). 7 tests d'étanchéité OK. FRONT : onglet « Mon agenda » dans espace-client/page.js (RDV à venir/passés, interventions, date_heure affiché Europe/Paris sans parseUTC, dates pures d'intervention via formatDateFR sans décalage). | ✅ mergé d188dd5 + f9b571d |
| 24/06 | AGENDA TRIGGER DOSSIER + SÉLECTEUR AGENCE ADMIN — feat/socle-agenda-suite. (1) SQL : trigger agenda_derive_tenant() amélioré → dérive l'agence du DOSSIER en priorité (dossier_id présent → agence du dossier, INSERT et UPDATE ; sinon agence fournie/sélecteur conservée ; sinon get_my_agence_id). Corrige un orphelin LATENT : un admin (agence_id NULL) créant un RDV AVEC dossier restait orphelin car le trigger dérivait du créateur, pas du dossier. NULL-safe service_role. 8 tests OK (T1 admin+dossier→agence du dossier = bug corrigé, T6 sync, T7 UPDATE garde l'agence, T8 cloisonnement). docs/sql/2026-06-24_agenda_trigger_dossier.sql. (2) Front : sélecteur d'agence admin sur les RDV 'autres' sans dossier (planning), OBLIGATOIRE avec message « Il manque une agence » ; envoi agence_id=dossier.agence_id partout (planning + fiche chantier, RDV + interventions) ; embed dossiers + agence_id ; prefill édition. Agente : jamais de sélecteur. | ✅ mergé (dans 06c67f6) |
| 24/06 | AGENDA created_by — feat/socle-agenda-suite. created_by uuid DEFAULT auth.uid() sur rendez_vous + interventions_artisans, FK profiles(id) ON DELETE SET NULL (informatif, ne bloque PAS deleteUser ≠ NO ACTION), nullable. Capture auto le créateur à l'INSERT (NULL pour service_role/sync). Immuable de fait (default ne touche pas l'UPDATE). Pas de backfill (260+9 existants NULL), pas de RLS, pas de front. delete_rule=SET NULL confirmé en vérif. ⚠️ DÉCOUVERTE : profiles.id A une FK vers auth.users (profiles_id_fkey) — l'audit créated_by s'était trompé en disant l'inverse ; cohérent avec le piège deleteUser de la boussole. | ✅ mergé 06c67f6 |
| 24/06 | SYNC ÉTAGE 2 — LOT 1 : Fix TDZ Google — feat/sync-tdz-fix (mergé bac38d7). Bug "Cannot access 'auth' before initialization" : variable const auth (utilisateur) masquée par un 2e const auth (client OAuth) plus bas dans le même scope. Fix chirurgical : client OAuth → oauthClient (sync/route.js + event/route.js), toutes utilisations en aval. Sync + delete Google ne plantent plus au runtime. Rename pur. ⚠️ Sync NON lançable (tenant + calendar_id = lots 2/4). | ✅ mergé bac38d7 |
| 24/06 | SYNC ÉTAGE 2 — LOT 2 : Structure de ciblage calendrier — feat/sync-ciblage-structure (mergé 0557fdd, 3 sous-lots). Remplace (à terme) le GOOGLE_CALENDAR_ID global par un ciblage souple multi-fournisseur/multi-calendrier. 2a : table cibles_calendrier (périmètre agence XOR perso via CHECK num_nonnulls=1 ; fournisseur CHECK google/outlook/icloud ; calendar_id ; compte_oauth_id→google_tokens partageable ; created_by DEFAULT auth.uid()), trigger cible_derive_societe (NULL-safe), RLS (SELECT=perso∪agence∪admin-société, WRITE=perso par propriétaire/agence par admin-société), REVOKE+grants. 8 tests OK. 2b : colonnes liaison profiles.cible_calendrier_defaut_id + rendez_vous.cible_id + interventions_artisans.cible_id (FK ON DELETE SET NULL). 4 tests OK. 2c : seed cible historique Martigues (compte OAuth Anne-Lise, sélectionnée sans ambiguïté par email @illico-travaux.com vs TEST AI @live.fr), défaut aux 3 AGENTES Martigues uniquement (pas admin/clients), rétro-mapping 260 RDV + 9 interventions. Simulation ROLLBACK avant COMMIT, CTE atomique anti-doublon. Vérifs CTP/3/0/0/260/9. ⚠️ INERTE : bascule = lots 3/4. | ✅ mergé 0557fdd |
| 25/06 | SYNC ÉTAGE 2 — LOT 3 : Sélecteur de calendrier (cible) sur les modales — feat/sync-cible-front-planning (3a + 3b). 3a planning : helper pur lib/cibles.js (determinerAgenceConcernee priorité dossier→navbar→sélecteur→agente ; resoudreCibleDefaut : défaut perso → 1ère cible d'agence active min(created_at) → null ; libelleCible), chargement cibles (RLS perso∪agence∪admin-société), sélecteur calendrier sur modales RDV+intervention (pré-sélection via helper, choix manuel protégé par useRef, désactivé en édition), sélecteur d'agence ÉTENDU (admin vue toutes : filtre dossiers RDV+interventions + assigne agence 'autres' + pilote résolution cible), writes cible_id, prefill édition, validations « Il manque une agence »/« Choisissez un calendrier » (admin vue toutes only, jamais une agente). 3b fiche chantier : calque réutilisant lib/cibles.js (pas de duplication), sélecteur sur 4 modales (RDV créa+édit mutualisé + intervention créa + intervention édit), agence toujours = dossier (pas de sélecteur agence, pas de validation). ⚠️ INERTE : push lit encore GOOGLE_CALENDAR_ID, écrire cible_id sans effet jusqu'au lot 4. | ✅ mergé ee4e0dd  |
| 26/06 | FIX MODALE RDV — fix/modale-rdv (3a planning + 3b chantiers). (1) Sélecteur de chantier persistant (retrait condition !dossier_id) : reste visible/modifiable en création+édition. (2) Artisans filtrés par devis accepté (statut='accepte') du dossier, pour RDV réception + interventions, partout (planning : filtre dossier_id+statut car devis global ; chantiers : statut seul car devis restreint au dossier). useMemo artisansRdvModale/artisansIntModale (planning). Reset artisan_id anti-fantôme dans les onChange manuels (dossier/type/agence), jamais en useEffect (préserve prefill édition). Anti-fantôme édition chantiers : artisan courant conservé dans la liste même sans devis accepté (donnée historique). Message « Aucun artisan avec devis signé sur ce chantier » si liste vide. Intervention = artisan devis accepté OBLIGATOIRE (strict, validé métier). Non touchés : DevisModal (création devis = tous artisans), modale CR (intervenantsDispo, sémantique participation), visite technique artisan/R2 (tous artisans). | ✅ mergé 38423f4|
| 26/06 | SYNC ÉTAGE 2 — LOT 4 COMPLET : Bascule push descendante — feat/sync-push-bascule (4a+4b+4c). 4a : push lit cible_id→cible (calendar_id + compte_oauth_id), tokens du détenteur (on('tokens') sur id compte, pas user), cible NULL/dossier→skip, dry-run GOOGLE_SYNC_DRY_RUN. 4c : câblage push fiche chantier (4 points RDV/interv create/edit, .select('id') ajouté), remplace l'ancien /sync, retrait bouton « Synchroniser » → indicateur passif, sonde GET conservée. 4b : scoping tenant — gate authentifiée (supabaseUser select('id') = contrôle appartenance agence/société) puis relecture admin bornée à l'id validé (préserve jointures dossier/client/artisan, sinon dégradées car RLS dossiers=référente). Writebacks + getCalendarForCible restent service_role. Testé dry-run : RDV+interv planning+chantier, summary complet, heure UTC correcte, multi-jours OK. ⚠️ Dry-run gardé actif en prod après merge ; activation réelle = désactiver le flag + test 1 RDV unique. | ✅ mergé feat/sync-push-bascule |
| 26/06 | RÉPARATION 175 RDV 'autres' — data/repare-rdv-autres-titre (fichier SQL mergé #290, réparation appliquée en base). Ancienne synchro Google (batch import 2026-05-15) avait mis le VRAI TITRE dans notes (au lieu de titre) + type_rdv='autres' sur 175 RDV (BNI Miramas, formations, etc. = vrais RDV divers, 'autres' est le BON type, PAS à re-typer). Discriminant : type_rdv='autres' AND (titre vide) AND notes remplies = 175, 0 faux positif (R1/R2/R3 ont titre NULL par design + vraies notes, non touchés ; 4 'autres' sains exclus). Réparation titre←trim(both espaces+\t\n\r from notes), notes←NULL, type INCHANGÉ. Backup table _backup_rdv_autres_titre_20260526 (à DROP plus tard). Simulation ROLLBACK avant COMMIT. Vérifs 175/0/0. | ✅ appliqué + mergé #290 |
| 26/06 | FIX AFFICHAGE titre 'autres' — fix/affichage-titre-autres (aae1168). Retrait du préfixe « Autre · » devant le titre des RDV 'autres' au planning (redondant depuis la réparation des titres). Grille (l.240-241) : 'autres' → (r.titre || cfg.label). Agenda 30j (l.300) : ajout cas 'autres' → (r.titre || cfg.label) (avant montrait « Autre · » vide, bug latent). R1/R2/R3 INCHANGÉS (gardent cfg.short). Push déjà correct pour 'autres' (titre seul ; ligne morte 'Autre - ' l.152 à nettoyer lot 5). 2 lignes. | ✅ mergé aae1168 |
| 28/06 | FIX RENDU PDF CR — fix/cr-pdf-pagination. Chevauchement page 1 des CR longs (R1 7 lots) : chaque section avait wrap:false → une section plus haute qu'une page débordait et se superposait (tableau d'identification + paragraphes écrasés). CAUSE : wrap:false (pas une contrainte « une page » — le récap R3 RecapitulatifPDF est séparé, non concerné). Fix buildCRDocument (app/api/pdf/route.js) : retrait wrap:false des sections → sécables, paginent sur N pages ; minPresenceAhead:60 sur l'en-tête (secHeader+secLine) → titre non orphelin sans rendre le contenu insécable ; renderKVTable param keepTogether → wrap:false UNIQUEMENT sur le tableau d'identification (borné ~8 lignes) ; tableau planning reste sécable (peut grandir). Règle absolue : aucun bloc wrap:false ne peut dépasser une page. Non touchés : RecapitulatifPDF (R3), cr/route.js (génération contenu). | ✅ mergé fix/cr-pdf-pagination |
| 28/06 | FIX RENDU PDF CR (2) — fix/cr-pdf-liste-insecable. Suite du fix pagination : un item de liste se coupait entre sa puce « – » (bas de page) et son texte (page suivante). Fix renderContent/flushList (app/api/pdf/route.js) : wrap:false sur chaque ITEM de liste (View CRS.listRow = puce + texte, borné 1-3 lignes) → puce et texte insécables ensemble ; liste globale reste sécable → pagine ENTRE les items. Règle absolue préservée : les 2 seuls wrap:false (item de liste + tableau identification) sont bornés < 1 page, aucune section de prose insécable. minPresenceAhead:60 (anti-orphelin titre) inchangé. | ✅ mergé fix/cr-pdf-liste-insecable |
| 28/06 | FIX RENDU PDF CR (3) — fix/cr-pdf-orphelin-pagevide. 2 défauts indépendants après les fix pagination. (1) Titre de section orphelin : en-tête groupé en insécable (wrap:false) AVEC son 1er bloc → titre emmène toujours son 1er élément. Garantie groupe < 1 page : listes APLATIES (flushList pousse chaque item comme bloc atomique au lieu d'un View englobant) → blocs[0] toujours atomique. renderContent renvoie {blocks, firstGroupable} ; tableau planning (peut grandir) = firstGroupable:false → en-tête seul + minPresenceAhead, pas de groupement. (2) Page fantôme finale : marginBottom:14 sur toutes sections → marginTop:14 sur toutes sauf la 1re → aucune marge après la dernière → plus de débordement de fin. Style secWrap retiré. Règle absolue maintenue (wrap:false uniquement sur blocs bornés <1 page : groupe en-tête+1er bloc, item liste, tableau identification). | ✅ mergé fix/cr-pdf-orphelin-pagevide |
| 29/06 | SYNC ÉTAGE 2 — LOT 5a : Lib calendrier Google (refactoring pur) — fix/calendar-lib-5a. Création app/lib/calendar/google.js centralisant la logique Google extraite de push/route.js À L'IDENTIQUE (zéro nettoyage) : rdvToGoogleEvent (typeLabels interne, quirks préservés), interventionToGoogleEvents (bloc inliné de POST extrait en fonction → [] si rien à pousser, sinon [principal, ...extras multi-jours]), buildIntervTimes, nextDay, getCalendarForCible, buildOAuthClientForCompte (refresh .eq('id', compteOauthId)), gcalWrite + deleteEvent (nouveau, gated DRY_RUN, non utilisé jusqu'au 5c), DRY_RUN centralisé. Lib crée son propre client service_role. push allégé de 187 lignes, importe la lib ; garde POST/scoping 4b/writebacks/extractToken. Non-régression PROUVÉE par dry-run diff vide : summaries identiques avec quirks intacts (double-espace « R2 -  | », espace initial « D2M »). Non touchés : sync (étage 3), event+front (5c), nettoyages (5b). | ✅ mergé fix/calendar-lib-5a |
| 29/06 | SYNC ÉTAGE 2 — LOT 5 COMPLET : Lib calendrier + nettoyages + bascule event — fix/calendar-lib-5a (5a+5b+5c). 5a : lib app/lib/calendar/google.js (extraction à l'identique de push, dry-run diff prouvé vide). 5b : 3 nettoyages summaries — RDV « R2 - Client x Artisan » (retrait pipe + double-espace, assemblage [type,client].filter.join(' - ') anti-tiret-pendouillant), intervention sans espace initial (garde le pipe), ligne morte 'Autre - ' supprimée. 5c : event/route.js basculé sur les cibles (body {googleEventId, cibleId}, getCalendarForCible + deleteEvent lib dry-run, garde-fou scoping RLS sur cibles_calendrier anti-forge, skips propres, retrait GOOGLE_CALENDAR_ID/getCalendar/googleapis) ; front passe cible_id (planning + chantiers, cible_id déjà chargé via select('*'), capturé avant delete DB). Stratégie (a) capability-based + garde-fou cible. Résout la dette event étage 2. sync laissée pour étage 3 (5d non fait). | ✅ mergé fix/calendar-lib-5a |
| 29/06 | SYNC ÉTAGE 2 — ACTIVATION RÉELLE Google. Dry-run désactivé (GOOGLE_SYNC_DRY_RUN retiré de Vercel + redéploiement). Testé en réel sur calendrier partagé Martigues : A1 UPDATE (RDV existant modifié → event Google mis à jour, pas de doublon, valide le droit d'écriture du compte sur le calendrier group), A2 INSERT (RDV créé → nouvel event), A3 DELETE (RDV supprimé → event retiré, valide le 5c). RDV de test nettoyé. GOOGLE_CALENDAR_ID supprimé de Vercel (résidu mort, plus lu depuis lot 4+5c). Sync descendante Google COMPLÈTE et vivante. | ✅ activé |
| 29/06 | LOT 6a — Généralisation google_tokens → comptes_oauth (feat/comptes-oauth-6a, mergé d5da4a8). RENAME table + colonnes (fournisseur CHECK google/outlook/icloud défaut google, compte_email, caldav_username/password/server nullables) + UNIQUE(user_id) → UNIQUE(user_id, fournisseur). FK cibles_calendrier + RLS + PK + policy suivent transparemment ; 3 comptes → fournisseur='google'. Code adapté : callback (+fournisseur:'google', onConflict (user_id,fournisseur)), lib (getCalendarForCible filtré fournisseur='google' = garde-fou), sync (5 requêtes), parametres/page.js:223 (indicateur connexion, manqué par l'audit). compte_email laissé NULL (exige élargir scope+reconnecter → lot 8). Structure multi-fournisseur prête, Google intact (testé 1 RDV réel). SQL docs/sql/2026-06-29_comptes_oauth.sql (simulation ROLLBACK 3/3/0/1/1/1 → COMMIT). | ✅ mergé d5da4a8 |
| 29/06 | SYNC ÉTAGE 2 — LOT 6b COMPLET : iCloud réel (CalDAV) — feat/dispatch-fournisseur-6b1 (6b-1+6b-2+6b-3). 6b-1 : couche dispatch getClientForCible(cibleId) (interface commune upsert/delete), Google passe par la nouvelle couche sans changer de comportement (resolveCible/buildGoogleCalendar/makeGoogleClientHandle extraits, dry-run diff vide prouvé). 6b-2 : moteur iCloud — crypto.js (AES-256-GCM, CALDAV_ENC_KEY env Vercel, app-specific password chiffré), icloud.js (builders ICS rdvToICS/interventionToICS via ical-generator, même mapping que Google DUPLIQUÉ → dette convergence à tracer ; client CalDAV tsdav Basic Auth ; handle upsert/delete, UID déterministe, ETag re-GET à la volée, externalId=URL CalDAV opaque dans google_event_id, 401=skip propre). push build fournisseur-aware. 6b-3 : SQL access_token DROP NOT NULL (iCloud sans token), compte iCloud test + cible perso (scripts jetables non commités, secret hors chat). Testé RÉEL : create/update/delete/idempotence/writeback OK + Google non régressé. | ✅ mergé 7ec328a |
| 29/06-01/07 | CALENDRIER LOT 8 « Mes calendriers » COMPLET — feat/lot8-mes-calendriers (mergé f368318). Écran self-service pour connecter des comptes (Google OAuth / iCloud CalDAV) et gérer ses cibles calendrier sans toucher la base. Composant partagé app/components/MesCalendriers.jsx (props profile/onError/onSucces), placement role-aware : agente → /profil, admin → /parametres>Intégrations (parametres admin-only). Sous-lots : 8a route /api/calendar/list (dispatch Google calendarList / iCloud fetchCalendars → {externalId,label}) ; 8b lecture + badge multi-fournisseur (présence compte, pas expiry — fix bug .gt('expiry_date')) ; 8c connexion iCloud front (valide via buildICloudClientRaw AVANT insert, encrypt serveur) + déconnexion (FK SET NULL → cibles inertes) ; 8d création/suppression cibles ; 8e cible par défaut (profiles.cible_calendrier_defaut_id) ; 8f colonne cibles.agenda_nom (nom lisible à la création + backfill Martigues='illiCO AL') + libellé éditable. 🔴 FIX RLS CONFIDENTIALITÉ (docs/sql/2026-06-29_rls_cibles_confidentialite.sql, appliqué prod) : clause admin voyait les cibles PERSO des agents (fuite) → visibilité agence gatée sur agence_id IS NOT NULL (perso = owner seul), cibles_write ALL séparée insert/update/delete. + ajust select agence conditionnel modales RDV/intervention (mergé 3453cb2 : 1 agence → select masqué+pré-rempli, multi → select). | ✅ mergé f368318 |
| 29/06 | OUTLOOK (calendrier) — ABANDONNÉ. Blocage ORGANISATIONNEL, pas technique. Compte @illico-travaux.com = tenant HEXARESO (franchiseur non contrôlé) bloque le consentement app externe. Compte perso outlook.com sans tenant Azure → portal.azure.com + entra.microsoft.com refusent la création d'app (« compte n'existe pas dans ce client »). Mapping partagé + dispatch maison prêts (pattern REST direct comme email.js en client_credentials app-only) SI un tenant Azure devient dispo ou admin-consent HEXARESO obtenu. | ⏸ abandonné |
| 30/06-01/07 | SOFT DELETE AGENTE — feat/soft-delete-agente (mergé 69b2e39). On DÉSACTIVE une agente au lieu de la supprimer (réversible), pour préserver l'attribution des dossiers + l'historique comptable. Résout le bug « Database error deleting user » (7 FK profiles NO ACTION bloquaient auth.admin.deleteUser). Colonne profiles.actif (docs/sql/2026-06-30_soft_delete_agente.sql). Route /api/agente-statut POST {id,actif} → ban/unban Supabase Auth (ban_duration '876000h'|'none') + update actif. auth-context fetchProfile déconnecte (signOut) une session dont actif=false (double blocage : ban = nouvelles connexions, signOut = session en cours ; gracieux tant que colonne absente : undefined≠false). UI parametres>Équipe Désactiver/Réactiver + badge. Filtres : chantiers/clients → .eq('actif',true) (assignation) ; parametres+finances → toutes (historique). Testé E2E (login normal OK, TEST AI bloquée à la connexion, réactivation OK, attribution préservée). NB : fix FK hard-delete (docs/sql/2026-06-30_fix_delete_agente.sql : 6 FK→SET NULL, factures_agente→CASCADE) appliqué prod AVANT, gardé comme filet de sécurité (branche hard-delete abandonnée). | ✅ mergé 69b2e39 |
| 01/07 | NETTOYAGE COMPTES TEST — TEST AI (a6f50d61, live.fr) + TEST 2 AGENTE (f0194df8, outlook.com) supprimés via dashboard Supabase Auth (auth+profiles cascade, factures CASCADE, comptes_oauth CASCADE). Redevances de test supprimées par id (delete where agente_id='a6f50d61...'). Reste 3 vraies agentes : Anne-Lise, Manon Montpellier, Marie Marseille. ⚠️ PIÈGE NOTÉ : le dashboard Supabase ne propage PAS les cascades SET NULL (redevances gardaient l'ancien id au lieu de NULL → 4 pointeurs cassés nettoyés à la main). Pour un vrai hard delete futur (RGPD) : script admin.deleteUser + vérif cascades, PAS le dashboard. | ✅ appliqué |
| 01/07 | TS-1 — RE-VENTILATION courtage→AMO (cas AMO) — feat/ts1-reventilation-amo (mergé 227622f). Règle validée Marine : devis dont date_signature > pivot (= suivi_financier.date_paiement ligne honoraires_courtage réglée) = TS. En AMO = re-ventilation, TOTAL INCHANGÉ : courtage des devis TS bascule en AMO. courtage = Σ(signés ≤ pivot) × taux_court ; AMO = Σ(≤pivot) × taux_amo + Σ(>pivot) × (taux_court+taux_amo). finance.js (SOURCE PROTÉGÉE, additif) : getPivotCourtage + sousTotalApresPivot (helpers) ; honorairesCore + {totalHTApres=0, totalTTCApres=0} (défaut 0 = calcul actuel STRICT, non-régression prouvée algébriquement + test node) ; 3 wrappers calculent apres sur leur périmètre si amo+pivot. Comparaison STRICTE (jour pivot = non-TS). Devis sans date_signature → ≤ pivot. Plumbing (commits 7fbe14b + 05f8308) : suiviFinancier threadé jusqu'à RecapHonoraires.js dans les 3 chemins PDF (route.js recapitulatif + recapitulatif_prev, restitution.js) — le récap standalone (type recapitulatif_prev = fichier Recap_Financier, nom trompeur) chargeait [] → corrigé. Testé RÉEL sur Jadras 2026-AM-001 : courtage 8090,09→7664,26 (−425,83 = 7097,20 TTC devis TS × 6%), total honoraire 17528,53 inchangé, frais conso 375 au total chantier (jamais dans honoraires), 3 PDF cohérents. | ✅ mergé 227622f |
| 09/07 | TS-2a — SOCLE SQL courtage TS — feat/ts2-courtage-ts (docs/sql/2026-07-09_ts2_courtage_ts.sql, appliqué prod). 3 opérations additives/réversibles : (1) 'honoraires_courtage_ts' ajouté au CHECK type_echeance de suivi_financier ; (2) index unique partiel suivi_financier_unique_sans_artisan relâché (WHERE artisan_id IS NULL AND type_echeance <> 'honoraires_courtage_ts' → autorise plusieurs lignes TS : L1 close + L2 ouverte) ; (3) RPC suivi_courtage_ts_upsert(p_dossier_id, p_montant) SECURITY INVOKER : UPDATE la ligne TS non-payée sinon INSERT (GET DIAGNOSTICS ROW_COUNT), DELETE si montant≤0. Simulé BEGIN…ROLLBACK avec test T1→T4 (INSERT 500 → UPDATE 800 sans doublon → règle L1 + 2e TS = L2 créée L1 close → montant 0 = DELETE ligne vide L1 intacte) : 4/4 conformes. Puis COMMIT réel + vérif APRÈS (CHECK 11 valeurs, index relâché, RPC security_definer=false). | ✅ appliqué |
| 09/07 | TS-2b — CODE courtage TS (calcul + déclenchement + affichage écran) — feat/ts2-courtage-ts (mergé 52f181e ; commits 5b2d711 code + 52236c4 garde-fou). Cas courtage-only = ÉCHELONNEMENT de l'encaissement (pas un supplément au total). finance.js (additif, non-régression PROUVÉE 30 comparaisons OLD/NEW identiques) : export getPivotCourtage+sousTotalApresPivot (corps inchangé) + calculateCourtageTS(dossier) → {montantTSttc, courtageInitialTtc, courtageTotalTtc}, fonction pure gate typologie='courtage'. Déclenchement AUTO : declencherCourtageTS() branché après chargerDevis() dans changerStatutDevis (statut accepte) ET uploadDevisSigne. Recompute IDEMPOTENT : montant ligne non-payée = montantTSttc − Σ(TS déjà réglés) → suivi_courtage_ts_upsert. Cochage par id (setCourtageTSPaye, jamais suivi_toggle_honoraires qui fige le montant). Affichage : courtage-only AVEC TS → « Honoraires courtage — initial » (courtageInitialTtc hors TS) + 1 EcheanceRow par ligne TS (L1/L2) + total (= base×taux inchangé). Sans TS / AMO → inchangé. GARDE-FOU pivot (52236c4) : EcheanceRow props lock/lockMsg rétro-compatibles ; dé-cochage du courtage initial BLOQUÉ tant que suiviCourtageTS.length>0 (sinon suivi_toggle_honoraires DELETE la ligne = perte du pivot). Pas de double comptage KPI (lignes TS = overlay d'encaissement, CA courtage reconnu via ligne initiale base complète). Testé réel. | ✅ mergé 52f181e |
| 09/07 | TS-2c — PDF ventilation courtage initial/TS — feat/ts2-pdf (mergé 4db5941). RecapHonoraires.js (composant PARTAGÉ Suivi_Financier + Recap_Financier + DossierSuivi) : pour courtage-only AVEC lignes honoraires_courtage_ts, REMPLACE la ligne courtage unique par « Honoraires courtage — initial » + 1 ligne par TS + total inchangé. ⚠️ Piège évité : preview (Recap) calcule base recu+accepte ≠ base signés de calculateCourtageTS → dérive initial = courtageReel − Σ(montants TS PERSISTÉS) → total ne bouge jamais, invariant initial+ΣTS=courtageReel exact en preview ET réel. Statut : Suivi_Financier/DossierSuivi (réel) affichent « réglé le X / en attente » ; Recap_Financier (preview) sans statut. finance.js INTOUCHÉ (lecture seule). Non-régression AMO + courtage-sans-TS byte-identique (chemin ligneHon unique). Test node arithmétique 12/12. Testé réel, total identique. → **Feature TS COMPLÈTE** (AMO + courtage-only, écran + PDF). | ✅ mergé 4db5941 |

---

> **Note d'archive** : les consignes process permanentes, l'état courant et la prochaine action vivent désormais dans `07_boussole_multitenant.md`. Ce journal n'est conservé que pour la traçabilité historique (commits, fichiers SQL, raisonnements de chaque lot).